#ifndef LIBRARIES_NODE_V8_H
#define LIBRARIES_NODE_V8_H

#include <exec/types.h>

/*
 * Node-specific compatibility surface layered over the pristine shared V8
 * engine.  Handles are deliberately opaque at the AROS library ABI.
 */

#endif
