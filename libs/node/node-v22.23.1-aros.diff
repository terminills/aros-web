diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/configure.py node-v22.23.1/configure.py
--- node-v22.23.1/configure.py	2026-06-23 11:27:38.000000000 -0500
+++ node-v22.23.1/configure.py	2026-07-26 12:11:56.876427176 -0500
@@ -943,6 +943,12 @@
     help='do not use V8 includes from the bundled deps folder. ' +
          '(This mode is not officially supported for regular applications)')
 
+parser.add_argument('--shared-node-v8',
+    action='store_true',
+    dest='shared_node_v8',
+    default=False,
+    help='route Node-specific V8 API deltas through node-v8.library')
+
 parser.add_argument('--verbose',
     action='store_true',
     dest='verbose',
@@ -1737,6 +1743,9 @@
   o['variables']['v8_trace_maps'] = 1 if options.trace_maps else 0
   o['variables']['node_use_v8_platform'] = b(not options.without_v8_platform)
   o['variables']['node_use_bundled_v8'] = b(not options.without_bundled_v8)
+  o['variables']['node_use_node_v8_library'] = b(options.shared_node_v8)
+  o['variables']['node_aros_libraries'] = ['-lusergroup'] if options.shared_node_v8 else []
+  o['variables']['node_v8_libraries'] = ['-lnode-v8'] if options.shared_node_v8 else []
   o['variables']['force_dynamic_crt'] = 1 if options.shared else 0
   o['variables']['node_enable_d8'] = b(options.enable_d8)
   o['variables']['node_enable_v8windbg'] = b(options.enable_v8windbg)
@@ -2336,6 +2345,19 @@
 # Add configurations to target defaults
 output['configurations'] = configurations
 
+if options.cross_compiling:
+  # Shared-library paths describe the target sysroot.  Applying them to host
+  # tools makes cross builds compile generators against target headers and
+  # attempt to link target archives with the host linker.
+  target_link_settings = {}
+  for setting in ('include_dirs', 'libraries'):
+    if output[setting]:
+      target_link_settings[setting] = output[setting]
+      output[setting] = []
+  if target_link_settings:
+    output.setdefault('target_conditions', []).append(
+        ['_toolset=="target"', target_link_settings])
+
 output = {
   'variables': variables,
   'target_defaults': output,
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/cares/config/linux/ares_config.h node-v22.23.1/deps/cares/config/linux/ares_config.h
--- node-v22.23.1/deps/cares/config/linux/ares_config.h	2026-06-23 11:27:39.000000000 -0500
+++ node-v22.23.1/deps/cares/config/linux/ares_config.h	2026-07-25 19:19:39.118058420 -0500
@@ -570,4 +570,38 @@
 /* #undef _LARGE_FILES */
 
 /* Define to `unsigned int' if <sys/types.h> does not define. */
-/* #undef size_t */
\ No newline at end of file
+/* #undef size_t */
+
+/*
+ * Node's GYP generator has no AROS target identity, so the port temporarily
+ * uses the Linux configuration. Remove facilities that AROS does not expose;
+ * c-ares then selects its portable select() backend.
+ */
+#ifdef __AROS__
+# undef HAVE_EPOLL
+# undef HAVE_SYS_EPOLL_H
+# undef HAVE_POLL
+# undef HAVE_POLL_H
+# undef HAVE_PIPE2
+# undef HAVE_GETRANDOM
+# undef HAVE_SYS_RANDOM_H
+# define HAVE_ARC4RANDOM_BUF 1
+# undef HAVE_GETSERVBYPORT_R
+# undef GETSERVBYPORT_R_ARGS
+# undef HAVE_IF_INDEXTONAME
+# undef HAVE_IF_NAMETOINDEX
+# undef HAVE_GETIFADDRS
+# undef HAVE_IFADDRS_H
+/*
+ * gethostname() is not a libc call on AROS: the only definition is
+ * bsdsocket.library's inline stub, which jumps through node.library's
+ * SocketBase - NULL whenever no TCP/IP stack is running. ares_init_options()
+ * called it at channel setup (require('dns')) and trapped there before any
+ * lookup was even asked for. uv_os_gethostname() answers "AROS" on this
+ * platform anyway, so no domain could ever be derived from it.
+ */
+# undef HAVE_GETHOSTNAME
+# ifndef INET6_ADDRSTRLEN
+#  define INET6_ADDRSTRLEN 46
+# endif
+#endif
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/llhttp/llhttp.gyp node-v22.23.1/deps/llhttp/llhttp.gyp
--- node-v22.23.1/deps/llhttp/llhttp.gyp	2026-06-23 11:27:39.000000000 -0500
+++ node-v22.23.1/deps/llhttp/llhttp.gyp	2026-07-26 11:27:49.880191623 -0500
@@ -17,6 +17,11 @@
       'sources': [
         '<@(llhttp_sources)',
       ],
+      'conditions': [
+        [ 'node_use_bundled_v8=="false" and node_shared=="false"', {
+          'cflags': [ '-fno-pic' ],
+        }],
+      ],
     },
   ]
 }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/openssl/openssl/crypto/dso/dso_dlfcn.c node-v22.23.1/deps/openssl/openssl/crypto/dso/dso_dlfcn.c
--- node-v22.23.1/deps/openssl/openssl/crypto/dso/dso_dlfcn.c	2026-06-23 11:27:40.000000000 -0500
+++ node-v22.23.1/deps/openssl/openssl/crypto/dso/dso_dlfcn.c	2026-07-26 12:26:26.113130609 -0500
@@ -19,6 +19,14 @@
 #include "dso_local.h"
 #include "internal/e_os.h"
 
+#ifdef __AROS__
+#undef DSO_DLFCN
+DSO_METHOD *DSO_METHOD_openssl(void)
+{
+    return NULL;
+}
+#endif
+
 #ifdef DSO_DLFCN
 
 #ifdef HAVE_DLFCN_H
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/simdjson/simdjson.h node-v22.23.1/deps/simdjson/simdjson.h
--- node-v22.23.1/deps/simdjson/simdjson.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/simdjson/simdjson.h	2026-07-25 20:14:31.905043506 -0500
@@ -4351,7 +4351,7 @@
 #endif
 
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__AROS__)
 /**
  * A class representing a memory-mapped file with padding.
  * It is only available on non-Windows platforms, as Windows has different APIs for memory mapping.
@@ -4688,7 +4688,7 @@
 #include <climits>
 #include <cwchar>
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__AROS__)
 #include <fcntl.h>
 #include <stdio.h>
 #include <sys/mman.h>
@@ -5053,7 +5053,7 @@
 }
 
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__AROS__)
 simdjson_inline padded_memory_map::padded_memory_map(const char *filename) noexcept {
 
     int fd = open(filename, O_RDONLY);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/uvwasi/src/clocks.c node-v22.23.1/deps/uvwasi/src/clocks.c
--- node-v22.23.1/deps/uvwasi/src/clocks.c	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/uvwasi/src/clocks.c	2026-07-25 19:20:48.315617538 -0500
@@ -123,6 +123,8 @@
 uvwasi_errno_t uvwasi__clock_gettime_process_cputime(uvwasi_timestamp_t* time) {
 #if defined(_WIN32)
   UVWASI__WIN_TIME_AND_RETURN(GetCurrentProcess(), GetProcessTimes, *time);
+#elif defined(__AROS__)
+  return UVWASI_ENOSYS;
 #elif defined(CLOCK_PROCESS_CPUTIME_ID) && \
       !defined(__APPLE__)               && \
       !defined(__sun)
@@ -138,7 +140,8 @@
   UVWASI__WIN_TIME_AND_RETURN(GetCurrentThread(), GetThreadTimes, *time);
 #elif defined(__APPLE__)
   UVWASI__OSX_THREADTIME_AND_RETURN(*time);
-#elif defined(CLOCK_THREAD_CPUTIME_ID) && !defined(__sun) && !defined(__PASE__)
+#elif defined(CLOCK_THREAD_CPUTIME_ID) && !defined(__sun) && \
+      !defined(__PASE__) && !defined(__AROS__)
   UVWASI__CLOCK_GETTIME_AND_RETURN(CLOCK_THREAD_CPUTIME_ID, *time);
 #else
 # if defined(RUSAGE_LWP)
@@ -157,7 +160,8 @@
   UVWASI__WIN_GETRES_AND_RETURN(*time);
 #elif defined(CLOCK_PROCESS_CPUTIME_ID) && \
       !defined(__APPLE__)               && \
-      !defined(__sun)
+      !defined(__sun)                   && \
+      !defined(__AROS__)
   UVWASI__CLOCK_GETRES_AND_RETURN(CLOCK_PROCESS_CPUTIME_ID, *time);
 #else
   UVWASI__SLOW_GETRES_AND_RETURN(*time);
@@ -170,7 +174,8 @@
   UVWASI__WIN_GETRES_AND_RETURN(*time);
 #elif defined(__APPLE__)
   UVWASI__SLOW_GETRES_AND_RETURN(*time);
-#elif defined(CLOCK_THREAD_CPUTIME_ID) && !defined(__sun) && !defined(__PASE__)
+#elif defined(CLOCK_THREAD_CPUTIME_ID) && !defined(__sun) && \
+      !defined(__PASE__) && !defined(__AROS__)
   UVWASI__CLOCK_GETTIME_AND_RETURN(CLOCK_THREAD_CPUTIME_ID, *time);
 #elif defined(RUSAGE_THREAD) || defined(RUSAGE_LWP)
   UVWASI__SLOW_GETRES_AND_RETURN(*time);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/uvwasi/src/uvwasi.c node-v22.23.1/deps/uvwasi/src/uvwasi.c
--- node-v22.23.1/deps/uvwasi/src/uvwasi.c	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/uvwasi/src/uvwasi.c	2026-07-25 19:15:49.002530066 -0500
@@ -36,8 +36,8 @@
 #include "wasi_serdes.h"
 #include "debug.h"
 
-/* IBMi PASE does not support posix_fadvise() */
-#ifdef __PASE__
+/* IBM i PASE and AROS do not support posix_fadvise(). */
+#if defined(__PASE__) || defined(__AROS__)
 # undef POSIX_FADV_NORMAL
 #endif
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/include/cppgc/internal/write-barrier.h node-v22.23.1/deps/v8/include/cppgc/internal/write-barrier.h
--- node-v22.23.1/deps/v8/include/cppgc/internal/write-barrier.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/include/cppgc/internal/write-barrier.h	2026-09-11 22:54:39.346292217 -0500
@@ -115,7 +115,13 @@
   // The FlagUpdater class allows cppgc internal to update
   // |write_barrier_enabled_|.
   class FlagUpdater;
-  static bool IsEnabled() { return write_barrier_enabled_.MightBeEntered(); }
+  static bool IsEnabled() {
+#if V8_OS_AROS
+    return WriteBarrierEnabledStorage().MightBeEntered();
+#else
+    return write_barrier_enabled_.MightBeEntered();
+#endif
+  }
 
  private:
   WriteBarrier() = delete;
@@ -150,7 +156,13 @@
       HeapHandle* heap_handle);
 #endif  // CPPGC_YOUNG_GENERATION
 
+#if V8_OS_AROS
+  // Keep the process-wide barrier flag in v8.library and make inline Blink
+  // callers import an accessor function, not a duplicate data symbol.
+  static AtomicEntryFlag& WriteBarrierEnabledStorage();
+#else
   static AtomicEntryFlag write_barrier_enabled_;
+#endif
 };
 
 template <WriteBarrier::Type type>
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/include/cppgc/process-heap-statistics.h node-v22.23.1/deps/v8/include/cppgc/process-heap-statistics.h
--- node-v22.23.1/deps/v8/include/cppgc/process-heap-statistics.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/include/cppgc/process-heap-statistics.h	2026-09-11 22:54:39.346456734 -0500
@@ -18,15 +18,31 @@
 class V8_EXPORT ProcessHeapStatistics final {
  public:
   static size_t TotalAllocatedObjectSize() {
+#if V8_OS_AROS
+    return TotalAllocatedObjectSizeStorage().load(std::memory_order_relaxed);
+#else
     return total_allocated_object_size_.load(std::memory_order_relaxed);
+#endif
   }
   static size_t TotalAllocatedSpace() {
+#if V8_OS_AROS
+    return TotalAllocatedSpaceStorage().load(std::memory_order_relaxed);
+#else
     return total_allocated_space_.load(std::memory_order_relaxed);
+#endif
   }
 
  private:
+#if V8_OS_AROS
+  // AROS keeps the V8 engine in v8.library. Inline cppgc users in Chromium
+  // must reach the library-owned counters through function imports instead of
+  // creating executable-side data relocations for these process globals.
+  static std::atomic_size_t& TotalAllocatedSpaceStorage();
+  static std::atomic_size_t& TotalAllocatedObjectSizeStorage();
+#else
   static std::atomic_size_t total_allocated_space_;
   static std::atomic_size_t total_allocated_object_size_;
+#endif
 
   friend class internal::ProcessHeapStatisticsUpdater;
 };
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/include/v8config.h node-v22.23.1/deps/v8/include/v8config.h
--- node-v22.23.1/deps/v8/include/v8config.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/include/v8config.h	2026-09-11 22:54:39.346619438 -0500
@@ -67,6 +67,7 @@
 //
 //  V8_OS_ANDROID       - Android
 //  V8_OS_BSD           - BSDish (macOS, Net/Free/Open/DragonFlyBSD)
+//  V8_OS_AROS          - AROS Research Operating System
 //  V8_OS_CYGWIN        - Cygwin
 //  V8_OS_DRAGONFLYBSD  - DragonFlyBSD
 //  V8_OS_FREEBSD       - FreeBSD
@@ -84,7 +85,12 @@
 //  V8_OS_AIX           - AIX
 //  V8_OS_WIN           - Microsoft Windows
 
-#if defined(__ANDROID__)
+#if defined(__AROS__)
+# define V8_OS_AROS 1
+# define V8_OS_POSIX 1
+# define V8_OS_STRING "aros"
+
+#elif defined(__ANDROID__)
 # define V8_OS_ANDROID 1
 # define V8_OS_LINUX 1
 # define V8_OS_POSIX 1
@@ -168,6 +174,7 @@
 // -----------------------------------------------------------------------------
 // Operating system detection (target)
 //
+//  V8_TARGET_OS_AROS
 //  V8_TARGET_OS_ANDROID
 //  V8_TARGET_OS_FUCHSIA
 //  V8_TARGET_OS_IOS
@@ -181,7 +188,8 @@
 #ifdef V8_HAVE_TARGET_OS
 
 // The target OS is provided, just check that at least one known value is set.
-# if !defined(V8_TARGET_OS_ANDROID) \
+# if !defined(V8_TARGET_OS_AROS) \
+  && !defined(V8_TARGET_OS_ANDROID) \
   && !defined(V8_TARGET_OS_FUCHSIA) \
   && !defined(V8_TARGET_OS_IOS) \
   && !defined(V8_TARGET_OS_LINUX) \
@@ -193,7 +201,8 @@
 
 #else  // V8_HAVE_TARGET_OS
 
-# if defined(V8_TARGET_OS_ANDROID) \
+# if defined(V8_TARGET_OS_AROS) \
+  || defined(V8_TARGET_OS_ANDROID) \
   || defined(V8_TARGET_OS_FUCHSIA) \
   || defined(V8_TARGET_OS_IOS) \
   || defined(V8_TARGET_OS_LINUX) \
@@ -204,6 +213,10 @@
 # endif
 
 // Fall back to the detected host OS.
+#ifdef V8_OS_AROS
+# define V8_TARGET_OS_AROS
+#endif
+
 #ifdef V8_OS_ANDROID
 # define V8_TARGET_OS_ANDROID
 #endif
@@ -230,7 +243,9 @@
 
 #endif  // V8_HAVE_TARGET_OS
 
-#if defined(V8_TARGET_OS_ANDROID)
+#if defined(V8_TARGET_OS_AROS)
+# define V8_TARGET_OS_STRING "aros"
+#elif defined(V8_TARGET_OS_ANDROID)
 # define V8_TARGET_OS_STRING "android"
 #elif defined(V8_TARGET_OS_FUCHSIA)
 # define V8_TARGET_OS_STRING "fuchsia"
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/base/macros.h node-v22.23.1/deps/v8/src/base/macros.h
--- node-v22.23.1/deps/v8/src/base/macros.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/base/macros.h	2026-09-11 22:54:39.346860088 -0500
@@ -12,6 +12,13 @@
 #include "src/base/compiler-specific.h"
 #include "src/base/logging.h"
 
+#if V8_OS_AROS
+// exec/types.h exposes these legacy storage qualifiers in the global macro
+// namespace, where they collide with V8 enum values.
+#undef GLOBAL
+#undef REGISTER
+#endif
+
 // No-op macro which is used to work around MSVC's funky VA_ARGS support.
 #define EXPAND(x) x
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/base/platform/memory.h node-v22.23.1/deps/v8/src/base/platform/memory.h
--- node-v22.23.1/deps/v8/src/base/platform/memory.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/base/platform/memory.h	2026-09-11 22:54:39.347022271 -0500
@@ -19,13 +19,13 @@
 
 #if V8_OS_DARWIN
 #include <malloc/malloc.h>
-#else  // !V8_OS_DARWIN
+#elif !V8_OS_AROS
 #include <malloc.h>
-#endif  // !V8_OS_DARWIN
+#endif
 
-#if (V8_OS_POSIX && !V8_OS_AIX && !V8_OS_SOLARIS) || V8_OS_WIN
+#if (V8_OS_POSIX && !V8_OS_AIX && !V8_OS_AROS && !V8_OS_SOLARIS) || V8_OS_WIN
 #define V8_HAS_MALLOC_USABLE_SIZE 1
-#endif  // (V8_OS_POSIX && !V8_OS_AIX && !V8_OS_SOLARIS) || V8_OS_WIN
+#endif
 
 namespace v8::base {
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/base/platform/platform.cc node-v22.23.1/deps/v8/src/base/platform/platform.cc
--- node-v22.23.1/deps/v8/src/base/platform/platform.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/base/platform/platform.cc	2026-09-11 22:54:39.347482421 -0500
@@ -10,22 +10,35 @@
 namespace {
 
 // A pointer to current thread's stack beginning.
+#if !V8_OS_AROS
 thread_local void* thread_stack_start = nullptr;
+#endif
 
 }  // namespace
 
 // static
 Stack::StackSlot Stack::GetStackStartUnchecked() {
+#if V8_OS_AROS
+  // Hosted AROS Exec tasks are not in one-to-one correspondence with host
+  // pthread TLS contexts. Caching this value in compiler-emulated TLS can
+  // therefore return a different Exec task's stack bounds. The AROS
+  // pthread_getattr_np interposer reads the current task directly, so query it
+  // for every request.
+  return ObtainCurrentThreadStackStart();
+#else
   if (!thread_stack_start) {
     thread_stack_start = ObtainCurrentThreadStackStart();
   }
   return thread_stack_start;
+#endif
 }
 
 // static
 Stack::StackSlot Stack::GetStackStart() {
+#if !V8_OS_AROS
   DCHECK_IMPLIES(thread_stack_start,
                  thread_stack_start == ObtainCurrentThreadStackStart());
+#endif
   return GetStackStartUnchecked();
 }
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/base/platform/platform-posix.cc node-v22.23.1/deps/v8/src/base/platform/platform-posix.cc
--- node-v22.23.1/deps/v8/src/base/platform/platform-posix.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/base/platform/platform-posix.cc	2026-09-11 22:54:39.347233846 -0500
@@ -17,7 +17,18 @@
 #include <fcntl.h>
 #include <sched.h>  // for sched_yield
 #include <stdio.h>
+#if V8_OS_AROS
+#include <exec/memory.h>
+#include <exec/tasks.h>
+#include <proto/arossupport.h>
+#include <proto/exec.h>
+#undef Allocate
+#undef Remove
+#undef Signal
+#undef Wait
+#else
 #include <sys/mman.h>
+#endif
 #include <sys/stat.h>
 #include <sys/time.h>
 #include <sys/types.h>
@@ -54,7 +65,7 @@
 #if V8_OS_DARWIN
 #include <mach/mach.h>
 #include <malloc/malloc.h>
-#else
+#elif !V8_OS_AROS
 #include <malloc.h>
 #endif
 
@@ -68,27 +79,29 @@
 #include <sys/resource.h>
 #endif
 
-#if !defined(_AIX) && !defined(V8_OS_FUCHSIA)
+#if !defined(_AIX) && !defined(V8_OS_FUCHSIA) && !V8_OS_AROS
 #include <sys/syscall.h>
 #endif
 
+#if V8_OS_AROS
+#define PROT_NONE 0
+#define PROT_READ 1
+#define PROT_WRITE 2
+#define PROT_EXEC 4
+#define MAP_SHARED 1
+#define MAP_PRIVATE 2
+#define MAP_FIXED 0x10
+#define MAP_ANONYMOUS 0x20
+#define MAP_NORESERVE 0x4000
+#define MAP_FAILED ((void*)-1)
+#define MADV_DONTNEED 4
+#endif
+
 #if V8_OS_FREEBSD || V8_OS_DARWIN || V8_OS_OPENBSD || V8_OS_SOLARIS
 #define MAP_ANONYMOUS MAP_ANON
 #endif
 
-/*
- * NOTE: illumos starting with illumos#14418 (pushed April 20th, 2022)
- * prototypes madvise(3C) properly with a `void *` first argument.
- * The only way to detect this outside of configure-time checking is to
- * check for the existence of MEMCNTL_SHARED, which gets defined for the first
- * time in illumos#14418 under the same circumstances save _STRICT_POSIX, which
- * thankfully neither Solaris nor illumos builds of Node or V8 do.
- *
- * If some future illumos push changes the MEMCNTL_SHARED assumptions made
- * above, the illumos check below will have to be revisited.  This check
- * will work on both pre-and-post illumos#14418 illumos environments.
- */
-#if defined(V8_OS_SOLARIS) && !(defined(__illumos__) && defined(MEMCNTL_SHARED))
+#if defined(V8_OS_SOLARIS)
 #if (defined(_POSIX_C_SOURCE) && _POSIX_C_SOURCE > 2) || defined(__EXTENSIONS__)
 extern "C" int madvise(caddr_t, size_t, int);
 #else
@@ -104,11 +117,209 @@
 extern "C" void* __libc_stack_end;
 #endif
 
+#if V8_OS_AROS
+static int ArosCurrentTaskId() {
+  return static_cast<int>(GetETaskID(FindTask(nullptr)));
+}
+#endif
+
 namespace v8 {
 namespace base {
 
 namespace {
 
+#if V8_OS_AROS
+struct ArosMapping {
+  ArosMapping* next;
+  void* raw;
+  size_t raw_size;
+  uintptr_t mapping_start;
+  size_t mapping_size;
+  uintptr_t live_start;
+  uintptr_t live_end;
+  int fd;
+  off_t offset;
+  bool write_back;
+};
+
+ArosMapping* g_aros_mappings = nullptr;
+volatile unsigned char g_aros_mappings_lock = 0;
+
+void LockArosMappings() {
+  while (__atomic_test_and_set(&g_aros_mappings_lock, __ATOMIC_ACQUIRE)) {
+    __asm__ __volatile__("pause");
+  }
+}
+
+void UnlockArosMappings() {
+  __atomic_clear(&g_aros_mappings_lock, __ATOMIC_RELEASE);
+}
+
+bool TransferArosFile(int fd, off_t offset, void* data, size_t size,
+                      bool write_back) {
+  LockArosMappings();
+  off_t previous = ::lseek(fd, 0, SEEK_CUR);
+  bool ok = previous != static_cast<off_t>(-1) &&
+            ::lseek(fd, offset, SEEK_SET) != static_cast<off_t>(-1);
+  size_t transferred = 0;
+  while (ok && transferred < size) {
+    char* cursor = static_cast<char*>(data) + transferred;
+    ssize_t result = write_back ? ::write(fd, cursor, size - transferred)
+                                : ::read(fd, cursor, size - transferred);
+    if (result > 0) {
+      transferred += static_cast<size_t>(result);
+    } else if (!write_back && result == 0) {
+      memset(cursor, 0, size - transferred);
+      transferred = size;
+    } else if (result < 0 && errno == EINTR) {
+      continue;
+    } else {
+      ok = false;
+    }
+  }
+  if (previous != static_cast<off_t>(-1) &&
+      ::lseek(fd, previous, SEEK_SET) == static_cast<off_t>(-1)) {
+    ok = false;
+  }
+  UnlockArosMappings();
+  return ok;
+}
+
+void* ArosMmap(void* address, size_t size, int prot, int flags, int fd,
+               off_t offset) {
+  if (size == 0) {
+    errno = EINVAL;
+    return MAP_FAILED;
+  }
+
+  if (flags & MAP_FIXED) {
+    if (address == nullptr || address == MAP_FAILED) {
+      errno = EINVAL;
+      return MAP_FAILED;
+    }
+    if (fd >= 0) {
+      if (!TransferArosFile(fd, offset, address, size, false)) {
+        return MAP_FAILED;
+      }
+    } else {
+      memset(address, 0, size);
+    }
+    return address;
+  }
+
+  constexpr size_t kPageSize = 4096;
+  if (size > SIZE_MAX - (kPageSize - 1)) {
+    errno = ENOMEM;
+    return MAP_FAILED;
+  }
+  size_t raw_size = size + kPageSize - 1;
+  void* raw = AllocMem(raw_size, MEMF_PUBLIC | MEMF_CLEAR);
+  if (!raw) {
+    errno = ENOMEM;
+    return MAP_FAILED;
+  }
+  uintptr_t mapping_start =
+      (reinterpret_cast<uintptr_t>(raw) + kPageSize - 1) &
+      ~(static_cast<uintptr_t>(kPageSize) - 1);
+
+  if (fd >= 0 &&
+      !TransferArosFile(fd, offset, reinterpret_cast<void*>(mapping_start),
+                        size, false)) {
+    FreeMem(raw, raw_size);
+    return MAP_FAILED;
+  }
+
+  auto* mapping = static_cast<ArosMapping*>(
+      AllocMem(sizeof(ArosMapping), MEMF_PUBLIC | MEMF_CLEAR));
+  if (!mapping) {
+    FreeMem(raw, raw_size);
+    errno = ENOMEM;
+    return MAP_FAILED;
+  }
+  mapping->raw = raw;
+  mapping->raw_size = raw_size;
+  mapping->mapping_start = mapping_start;
+  mapping->mapping_size = size;
+  mapping->live_start = mapping_start;
+  mapping->live_end = mapping_start + size;
+  mapping->fd = fd;
+  mapping->offset = offset;
+  mapping->write_back =
+      fd >= 0 && (flags & MAP_SHARED) != 0 && (prot & PROT_WRITE) != 0;
+
+  LockArosMappings();
+  mapping->next = g_aros_mappings;
+  g_aros_mappings = mapping;
+  UnlockArosMappings();
+  return reinterpret_cast<void*>(mapping_start);
+}
+
+int ArosMunmap(void* address, size_t size) {
+  if (!address || address == MAP_FAILED || size == 0) {
+    errno = EINVAL;
+    return -1;
+  }
+
+  uintptr_t start = reinterpret_cast<uintptr_t>(address);
+  uintptr_t end = start + size;
+  if (end < start) end = UINTPTR_MAX;
+
+  LockArosMappings();
+  ArosMapping** link = &g_aros_mappings;
+  while (*link) {
+    ArosMapping* mapping = *link;
+    if (start >= mapping->live_start && start < mapping->live_end) {
+      if (start == mapping->live_start) {
+        mapping->live_start = end < mapping->live_end ? end : mapping->live_end;
+      } else if (end >= mapping->live_end) {
+        mapping->live_end = start;
+      } else {
+        UnlockArosMappings();
+        return 0;
+      }
+
+      if (mapping->live_start < mapping->live_end) {
+        UnlockArosMappings();
+        return 0;
+      }
+
+      *link = mapping->next;
+      UnlockArosMappings();
+      if (mapping->write_back) {
+        TransferArosFile(mapping->fd, mapping->offset,
+                         reinterpret_cast<void*>(mapping->mapping_start),
+                         mapping->mapping_size, true);
+      }
+      FreeMem(mapping->raw, mapping->raw_size);
+      FreeMem(mapping, sizeof(ArosMapping));
+      return 0;
+    }
+    link = &mapping->next;
+  }
+  UnlockArosMappings();
+  return 0;
+}
+
+int ArosMprotect(void* address, size_t size, int prot) {
+  USE(address);
+  USE(size);
+  USE(prot);
+  return 0;
+}
+
+int ArosMadvise(void* address, size_t size, int advice) {
+  if (advice == MADV_DONTNEED || advice == MADV_FREE) {
+    memset(address, 0, size);
+  }
+  return 0;
+}
+
+#define mmap ArosMmap
+#define munmap ArosMunmap
+#define mprotect ArosMprotect
+#define madvise ArosMadvise
+#endif
+
 // 0 is never a valid thread id.
 const pthread_t kNoThread = static_cast<pthread_t>(0);
 
@@ -317,6 +528,9 @@
 
 // static
 void* OS::GetRandomMmapAddr() {
+#if V8_OS_AROS
+  return nullptr;
+#else
   uintptr_t raw_addr;
   {
     MutexGuard guard(rng_mutex.Pointer());
@@ -534,6 +748,11 @@
 
 // static
 void OS::SetDataReadOnly(void* address, size_t size) {
+#if V8_OS_AROS
+  USE(address);
+  USE(size);
+  return;
+#else
   CHECK_EQ(0, reinterpret_cast<uintptr_t>(address) % CommitPageSize());
   CHECK_EQ(0, size % CommitPageSize());
 
@@ -836,6 +1055,8 @@
   return static_cast<int>(zx_thread_self());
 #elif V8_OS_SOLARIS
   return static_cast<int>(pthread_self());
+#elif V8_OS_AROS
+  return ArosCurrentTaskId();
 #else
   return static_cast<int>(reinterpret_cast<intptr_t>(pthread_self()));
 #endif
@@ -855,18 +1076,24 @@
 
 #if !defined(V8_OS_FUCHSIA)
 int OS::GetUserTime(uint32_t* secs, uint32_t* usecs) {
+#if V8_OS_AROS
+  *secs = 0;
+  *usecs = 0;
+  return -1;
+#else
   struct rusage usage;
 
   if (getrusage(RUSAGE_SELF, &usage) < 0) return -1;
   *secs = static_cast<uint32_t>(usage.ru_utime.tv_sec);
   *usecs = static_cast<uint32_t>(usage.ru_utime.tv_usec);
   return 0;
+#endif
 }
 #endif
 
 int OS::GetPeakMemoryUsageKb() {
-#if defined(V8_OS_FUCHSIA)
-  // Fuchsia does not implement getrusage()
+#if defined(V8_OS_FUCHSIA) || V8_OS_AROS
+  // Fuchsia and AROS do not implement getrusage().
   return -1;
 #else
   struct rusage usage;
@@ -987,6 +1214,14 @@
 #if defined(ANDROID) && !defined(V8_ANDROID_LOG_STDOUT)
   __android_log_vprint(ANDROID_LOG_ERROR, LOG_TAG, format, args);
 #else
+#if V8_OS_AROS
+  char debug_message[2048];
+  va_list debug_args;
+  va_copy(debug_args, args);
+  vsnprintf(debug_message, sizeof(debug_message), format, debug_args);
+  va_end(debug_args);
+  kprintf("[V8:stderr] %s", debug_message);
+#endif
   vfprintf(stderr, format, args);
 #endif
 }
@@ -1378,5 +1613,22 @@
 #undef MAP_ANONYMOUS
 #undef MADV_FREE
 
+#if V8_OS_AROS
+#undef mmap
+#undef munmap
+#undef mprotect
+#undef madvise
+#undef PROT_NONE
+#undef PROT_READ
+#undef PROT_WRITE
+#undef PROT_EXEC
+#undef MAP_SHARED
+#undef MAP_PRIVATE
+#undef MAP_FIXED
+#undef MAP_NORESERVE
+#undef MAP_FAILED
+#undef MADV_DONTNEED
+#endif
+
 }  // namespace base
 }  // namespace v8
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/base/sys-info.cc node-v22.23.1/deps/v8/src/base/sys-info.cc
--- node-v22.23.1/deps/v8/src/base/sys-info.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/base/sys-info.cc	2026-09-11 22:54:39.347601784 -0500
@@ -4,6 +4,16 @@
 
 #include "src/base/sys-info.h"
 
+#if V8_OS_AROS
+#include <exec/memory.h>
+#pragma push_macro("SCHED_RR")
+#undef SCHED_RR
+#include <aros/kernel.h>
+#pragma pop_macro("SCHED_RR")
+#include <proto/exec.h>
+#include <proto/kernel.h>
+#endif
+
 #if V8_OS_POSIX
 #include <sys/stat.h>
 #include <sys/time.h>
@@ -37,7 +47,12 @@
 
 // static
 int SysInfo::NumberOfProcessors() {
-#if V8_OS_OPENBSD
+#if V8_OS_AROS
+  APTR KernelBase = OpenResource("kernel.resource");
+  if (!KernelBase) return 1;
+  int result = KrnGetCPUCount();
+  return result > 0 ? result : 1;
+#elif V8_OS_OPENBSD
   int mib[2] = {CTL_HW, HW_NCPU};
   int ncpu = 0;
   size_t len = sizeof(ncpu);
@@ -63,7 +78,9 @@
 
 // static
 int64_t SysInfo::AmountOfPhysicalMemory() {
-#if V8_OS_DARWIN
+#if V8_OS_AROS
+  return static_cast<int64_t>(AvailMem(MEMF_TOTAL));
+#elif V8_OS_DARWIN
   int mib[2] = {CTL_HW, HW_MEMSIZE};
   int64_t memsize = 0;
   size_t len = sizeof(memsize);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/deoptimizer/translation-opcode.h node-v22.23.1/deps/v8/src/deoptimizer/translation-opcode.h
--- node-v22.23.1/deps/v8/src/deoptimizer/translation-opcode.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/deoptimizer/translation-opcode.h	2026-09-11 22:54:39.347739471 -0500
@@ -7,6 +7,11 @@
 
 #include "src/base/macros.h"
 
+#if V8_OS_AROS
+#pragma push_macro("REGISTER")
+#undef REGISTER
+#endif
+
 namespace v8 {
 namespace internal {
 
@@ -132,4 +137,8 @@
 }  // namespace internal
 }  // namespace v8
 
+#if V8_OS_AROS
+#pragma pop_macro("REGISTER")
+#endif
+
 #endif  // V8_DEOPTIMIZER_TRANSLATION_OPCODE_H_
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/execution/thread-id.cc node-v22.23.1/deps/v8/src/execution/thread-id.cc
--- node-v22.23.1/deps/v8/src/execution/thread-id.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/execution/thread-id.cc	2026-09-11 22:54:39.347866629 -0500
@@ -11,24 +11,38 @@
 
 namespace {
 
+#if !V8_OS_AROS
 thread_local int thread_id = 0;
 
 std::atomic<int> next_thread_id{1};
+#endif
 
 }  // namespace
 
 // static
 ThreadId ThreadId::TryGetCurrent() {
+#if V8_OS_AROS
+  return ThreadId(base::OS::GetCurrentThreadId());
+#else
   return thread_id == 0 ? Invalid() : ThreadId(thread_id);
+#endif
 }
 
 // static
 int ThreadId::GetCurrentThreadId() {
+#if V8_OS_AROS
+  // Compiler-emulated TLS follows the hosted execution context, not the AROS
+  // Exec task scheduled through it. Exec assigns every live ETask a unique
+  // identifier, which is the identity V8 needs for isolate and background
+  // compilation ownership checks.
+  return base::OS::GetCurrentThreadId();
+#else
   if (thread_id == 0) {
     thread_id = next_thread_id.fetch_add(1);
     CHECK_LE(1, thread_id);
   }
   return thread_id;
+#endif
 }
 
 }  // namespace internal
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/persistent-node.cc node-v22.23.1/deps/v8/src/heap/cppgc/persistent-node.cc
--- node-v22.23.1/deps/v8/src/heap/cppgc/persistent-node.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/persistent-node.cc	2026-09-11 22:54:39.348240598 -0500
@@ -118,7 +118,11 @@
 }
 
 bool PersistentRegion::IsCreationThread() {
+#if V8_OS_AROS
+  return true;
+#else
   return creation_thread_id_ == v8::base::OS::GetCurrentThreadId();
+#endif
 }
 
 PersistentRegionLock::PersistentRegionLock() {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/platform.cc node-v22.23.1/deps/v8/src/heap/cppgc/platform.cc
--- node-v22.23.1/deps/v8/src/heap/cppgc/platform.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/platform.cc	2026-09-11 22:54:39.348374989 -0500
@@ -26,6 +26,14 @@
 
 PageAllocator* g_page_allocator = nullptr;
 
+#if V8_OS_AROS
+// AROS Processes share one address space and therefore one v8.library data
+// image. Chromium still initializes cppgc once in every renderer Process, so
+// serialize that lifecycle and retain the single compatible allocator owned by
+// the resident engine image.
+v8::base::LazyMutex g_process_lifecycle_mutex = LAZY_MUTEX_INITIALIZER;
+#endif
+
 PageAllocator& CreateAllocatorIfNeeded(PageAllocator* page_allocator) {
   if (!page_allocator) {
     static v8::base::LeakyObject<v8::base::PageAllocator>
@@ -89,6 +97,11 @@
 
 void InitializeProcess(PageAllocator* page_allocator,
                        size_t desired_heap_size) {
+#if V8_OS_AROS
+  v8::base::MutexGuard lifecycle_guard(
+      internal::g_process_lifecycle_mutex.Pointer());
+#endif
+
 #if defined(V8_USE_ADDRESS_SANITIZER) && defined(V8_HOST_ARCH_64_BIT)
   // Retrieve asan's internal shadow memory granularity and check that Oilpan's
   // object alignment/sizes are multiple of this granularity. This is needed to
@@ -100,7 +113,22 @@
   CHECK_EQ(0u, internal::kAllocationGranularity % poisoning_granularity);
 #endif
 
+#if V8_OS_AROS
+  // The allocator supplied by Blink belongs to the caller's relocatable Chrome
+  // image. A resident v8.library cannot retain that object after the caller
+  // exits, and each AROS Process maps its own Chrome image at a different
+  // address. Own the shared cppgc allocator in the engine image instead.
+  auto& allocator = internal::CreateAllocatorIfNeeded(nullptr);
+#else
   auto& allocator = internal::CreateAllocatorIfNeeded(page_allocator);
+#endif
+
+#if V8_OS_AROS
+  if (internal::g_page_allocator) {
+    CHECK_EQ(&allocator, internal::g_page_allocator);
+    return;
+  }
+#endif
 
   CHECK(!internal::g_page_allocator);
   internal::GlobalGCInfoTable::Initialize(allocator);
@@ -110,6 +138,15 @@
   internal::g_page_allocator = &allocator;
 }
 
-void ShutdownProcess() { internal::g_page_allocator = nullptr; }
+void ShutdownProcess() {
+#if V8_OS_AROS
+  // v8.library is resident and its cppgc globals are shared by every AROS
+  // Process using the engine. One renderer must not invalidate the allocator
+  // while another renderer (or the browser) still owns cppgc heaps.
+  return;
+#else
+  internal::g_page_allocator = nullptr;
+#endif
+}
 
 }  // namespace cppgc
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.cc node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.cc
--- node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.cc	2026-09-11 22:54:39.348498370 -0500
@@ -6,7 +6,24 @@
 
 namespace cppgc {
 
+#if V8_OS_AROS
+namespace {
+
+std::atomic_size_t g_total_allocated_space{0};
+std::atomic_size_t g_total_allocated_object_size{0};
+
+}  // namespace
+
+std::atomic_size_t& ProcessHeapStatistics::TotalAllocatedSpaceStorage() {
+  return g_total_allocated_space;
+}
+
+std::atomic_size_t& ProcessHeapStatistics::TotalAllocatedObjectSizeStorage() {
+  return g_total_allocated_object_size;
+}
+#else
 std::atomic_size_t ProcessHeapStatistics::total_allocated_space_{0};
 std::atomic_size_t ProcessHeapStatistics::total_allocated_object_size_{0};
+#endif
 
 }  // namespace cppgc
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.h node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.h
--- node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/process-heap-statistics.h	2026-09-11 22:54:39.348619376 -0500
@@ -50,20 +50,40 @@
 
   // For cppgc::ProcessHeapStatistics
   static void IncreaseTotalAllocatedObjectSize(size_t delta) {
+#if V8_OS_AROS
+    ::cppgc::ProcessHeapStatistics::TotalAllocatedObjectSizeStorage().fetch_add(
+        delta, std::memory_order_relaxed);
+#else
     ::cppgc::ProcessHeapStatistics::total_allocated_object_size_.fetch_add(
         delta, std::memory_order_relaxed);
+#endif
   }
   static void DecreaseTotalAllocatedObjectSize(size_t delta) {
+#if V8_OS_AROS
+    ::cppgc::ProcessHeapStatistics::TotalAllocatedObjectSizeStorage().fetch_sub(
+        delta, std::memory_order_relaxed);
+#else
     ::cppgc::ProcessHeapStatistics::total_allocated_object_size_.fetch_sub(
         delta, std::memory_order_relaxed);
+#endif
   }
   static void IncreaseTotalAllocatedSpace(size_t delta) {
+#if V8_OS_AROS
+    ::cppgc::ProcessHeapStatistics::TotalAllocatedSpaceStorage().fetch_add(
+        delta, std::memory_order_relaxed);
+#else
     ::cppgc::ProcessHeapStatistics::total_allocated_space_.fetch_add(
         delta, std::memory_order_relaxed);
+#endif
   }
   static void DecreaseTotalAllocatedSpace(size_t delta) {
+#if V8_OS_AROS
+    ::cppgc::ProcessHeapStatistics::TotalAllocatedSpaceStorage().fetch_sub(
+        delta, std::memory_order_relaxed);
+#else
     ::cppgc::ProcessHeapStatistics::total_allocated_space_.fetch_sub(
         delta, std::memory_order_relaxed);
+#endif
   }
 };
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.cc node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.cc
--- node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.cc	2026-09-11 22:54:39.348755490 -0500
@@ -21,8 +21,21 @@
 namespace cppgc {
 namespace internal {
 
+#if V8_OS_AROS
+namespace {
+
+AtomicEntryFlag g_write_barrier_enabled;
+
+}  // namespace
+
+// static
+AtomicEntryFlag& WriteBarrier::WriteBarrierEnabledStorage() {
+  return g_write_barrier_enabled;
+}
+#else
 // static
 AtomicEntryFlag WriteBarrier::write_barrier_enabled_;
+#endif
 
 namespace {
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.h node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.h
--- node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.h	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc/write-barrier.h	2026-09-11 22:54:39.348887887 -0500
@@ -14,8 +14,20 @@
 
 class WriteBarrier::FlagUpdater final {
  public:
-  static void Enter() { write_barrier_enabled_.Enter(); }
-  static void Exit() { write_barrier_enabled_.Exit(); }
+  static void Enter() {
+#if V8_OS_AROS
+    WriteBarrierEnabledStorage().Enter();
+#else
+    write_barrier_enabled_.Enter();
+#endif
+  }
+  static void Exit() {
+#if V8_OS_AROS
+    WriteBarrierEnabledStorage().Exit();
+#else
+    write_barrier_enabled_.Exit();
+#endif
+  }
 
  private:
   FlagUpdater() = delete;
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/heap/cppgc-js/cpp-heap.cc node-v22.23.1/deps/v8/src/heap/cppgc-js/cpp-heap.cc
--- node-v22.23.1/deps/v8/src/heap/cppgc-js/cpp-heap.cc	2026-06-23 11:27:41.000000000 -0500
+++ node-v22.23.1/deps/v8/src/heap/cppgc-js/cpp-heap.cc	2026-09-11 22:54:39.348030215 -0500
@@ -564,6 +564,10 @@
 
 void CppHeap::AttachIsolate(Isolate* isolate) {
   CHECK(!in_detached_testing_mode_);
+#if defined(V8_OS_AROS)
+  if (isolate_ && isolate_ != isolate)
+    DetachIsolate();
+#endif
   CHECK_NULL(isolate_);
   isolate_ = isolate;
   heap_ = isolate->heap();
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/libsampler/sampler.cc node-v22.23.1/deps/v8/src/libsampler/sampler.cc
--- node-v22.23.1/deps/v8/src/libsampler/sampler.cc	2026-06-23 11:27:42.000000000 -0500
+++ node-v22.23.1/deps/v8/src/libsampler/sampler.cc	2026-09-11 22:54:39.349023601 -0500
@@ -312,6 +312,12 @@
   zx_handle_t profiled_thread_ = ZX_HANDLE_INVALID;
 };
 
+#elif V8_OS_AROS
+
+// AROS does not yet provide asynchronous pthread signals with a register
+// context, so sampling remains unavailable until that system contract exists.
+class Sampler::PlatformData {};
+
 #endif  // USE_SIGNALS
 
 #if defined(USE_SIGNALS)
@@ -671,6 +677,10 @@
 #undef ZX_THREAD_STATE_GENERAL_REGS
 #endif
 
+#elif V8_OS_AROS
+
+void Sampler::DoSample() {}
+
 #endif  // USE_SIGNALS
 
 }  // namespace sampler
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/libsampler/sampler.h node-v22.23.1/deps/v8/src/libsampler/sampler.h
--- node-v22.23.1/deps/v8/src/libsampler/sampler.h	2026-06-23 11:27:42.000000000 -0500
+++ node-v22.23.1/deps/v8/src/libsampler/sampler.h	2026-09-11 22:54:39.349175415 -0500
@@ -13,7 +13,7 @@
 #include "src/base/lazy-instance.h"
 #include "src/base/macros.h"
 
-#if V8_OS_POSIX && !V8_OS_CYGWIN && !V8_OS_FUCHSIA
+#if V8_OS_POSIX && !V8_OS_AROS && !V8_OS_CYGWIN && !V8_OS_FUCHSIA
 #define USE_SIGNALS
 #endif
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/logging/counters.cc node-v22.23.1/deps/v8/src/logging/counters.cc
--- node-v22.23.1/deps/v8/src/logging/counters.cc	2026-06-23 11:27:42.000000000 -0500
+++ node-v22.23.1/deps/v8/src/logging/counters.cc	2026-09-11 22:54:39.349301581 -0500
@@ -94,8 +94,19 @@
 #pragma clang diagnostic push
 #pragma clang diagnostic ignored "-Wexit-time-destructors"
 #endif
+#if V8_OS_AROS
+  // AROS' libsupc++ fallback runs non-trivial thread_local destructors from a
+  // pthread-key callback.  Destruction of this debug-only map has repeatedly
+  // reached free() with corrupted map storage during worker teardown.  Keep
+  // the per-thread state, but intentionally leave its tiny diagnostic map
+  // alive when the thread exits.
+  static thread_local auto* active_timer_storage =
+      new std::unordered_map<const TimedHistogram*, bool>;
+  auto& active_timer = *active_timer_storage;
+#else
   static thread_local std::unordered_map<const TimedHistogram*, bool>
       active_timer;
+#endif
 #if __clang__
 #pragma clang diagnostic pop
 #endif
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/v8/src/utils/allocation.cc node-v22.23.1/deps/v8/src/utils/allocation.cc
--- node-v22.23.1/deps/v8/src/utils/allocation.cc	2026-06-23 11:27:42.000000000 -0500
+++ node-v22.23.1/deps/v8/src/utils/allocation.cc	2026-09-11 22:54:39.349455087 -0500
@@ -32,7 +32,15 @@
 class PageAllocatorInitializer {
  public:
   PageAllocatorInitializer() {
+#if V8_OS_AROS
+    if (v8::Platform* platform = V8::GetCurrentPlatform()) {
+      page_allocator_ = platform->GetPageAllocator();
+    } else {
+      page_allocator_ = nullptr;
+    }
+#else
     page_allocator_ = V8::GetCurrentPlatform()->GetPageAllocator();
+#endif
     if (page_allocator_ == nullptr) {
       static base::LeakyObject<base::PageAllocator> default_page_allocator;
       page_allocator_ = default_page_allocator.get();
@@ -64,8 +72,18 @@
 }  // namespace
 
 v8::PageAllocator* GetPlatformPageAllocator() {
+#if V8_OS_AROS
+  if (v8::Platform* platform = V8::GetCurrentPlatform()) {
+    if (v8::PageAllocator* page_allocator = platform->GetPageAllocator()) {
+      return page_allocator;
+    }
+  }
+  static base::LeakyObject<base::PageAllocator> default_page_allocator;
+  return default_page_allocator.get();
+#else
   DCHECK_NOT_NULL(GetPageAllocatorInitializer()->page_allocator());
   return GetPageAllocatorInitializer()->page_allocator();
+#endif
 }
 
 v8::VirtualAddressSpace* GetPlatformVirtualAddressSpace() {
@@ -158,7 +176,11 @@
 size_t CommitPageSize() { return GetPlatformPageAllocator()->CommitPageSize(); }
 
 void* GetRandomMmapAddr() {
+#if V8_OS_AROS
+  return nullptr;
+#else
   return GetPlatformPageAllocator()->GetRandomMmapAddr();
+#endif
 }
 
 void* AllocatePages(v8::PageAllocator* page_allocator, void* hint, size_t size,
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/deps/zstd/lib/common/threading.h node-v22.23.1/deps/zstd/lib/common/threading.h
--- node-v22.23.1/deps/zstd/lib/common/threading.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/deps/zstd/lib/common/threading.h	2026-07-25 19:24:02.983380202 -0500
@@ -16,6 +16,15 @@
 
 #include "debug.h"
 
+#if defined(__AROS__)
+/*
+ * AROS exposes pthreads, but exec/types.h reserves BYTE as a signed ABI type
+ * while zstd uses BYTE as uint8_t. Keep zstd on its supported single-thread
+ * fallback until that namespace collision is isolated at the library seam.
+ */
+#  undef ZSTD_MULTITHREAD
+#endif
+
 #if defined(ZSTD_MULTITHREAD) && defined(_WIN32)
 
 /**
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/lib/internal/url.js node-v22.23.1/lib/internal/url.js
--- node-v22.23.1/lib/internal/url.js	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/lib/internal/url.js	2026-07-26 09:10:18.096670051 -0500
@@ -1572,7 +1572,17 @@
     }
   }
   // Fast-path: if there is no percent-encoding, avoid decodeURIComponent.
-  return StringPrototypeIncludes(pathname, '%') ? decodeURIComponent(pathname) : pathname;
+  const decodedPathname = StringPrototypeIncludes(pathname, '%') ?
+    decodeURIComponent(pathname) : pathname;
+  if (StringPrototypeCharCodeAt(decodedPathname, 0) === CHAR_FORWARD_SLASH) {
+    const volumeEnd = StringPrototypeIndexOf(decodedPathname, ':');
+    const firstSeparator = StringPrototypeIndexOf(decodedPathname, '/', 1);
+    if (volumeEnd > 1 &&
+        (firstSeparator === -1 || volumeEnd < firstSeparator)) {
+      return StringPrototypeSlice(decodedPathname, 1);
+    }
+  }
+  return decodedPathname;
 }
 
 function getPathBufferFromURLPosix(url) {
@@ -1592,9 +1602,28 @@
   // literally into the returned Buffer. We're going to do the best we can and
   // just interpret the input url as a sequence of bytes.
   const u8 = percentDecode(Buffer.from(pathname, 'utf8'));
-  return Buffer.from(TypedArrayPrototypeGetBuffer(u8),
-                     TypedArrayPrototypeGetByteOffset(u8),
-                     TypedArrayPrototypeGetByteLength(u8));
+  let byteOffset = TypedArrayPrototypeGetByteOffset(u8);
+  let byteLength = TypedArrayPrototypeGetByteLength(u8);
+  if (byteLength > 2 && u8[0] === CHAR_FORWARD_SLASH) {
+    let volumeEnd = -1;
+    let firstSeparator = -1;
+    for (let n = 1; n < byteLength; n++) {
+      if (u8[n] === CHAR_COLON) {
+        volumeEnd = n;
+        break;
+      }
+      if (u8[n] === CHAR_FORWARD_SLASH) {
+        firstSeparator = n;
+        break;
+      }
+    }
+    if (volumeEnd > 1 &&
+        (firstSeparator === -1 || volumeEnd < firstSeparator)) {
+      byteOffset++;
+      byteLength--;
+    }
+  }
+  return Buffer.from(TypedArrayPrototypeGetBuffer(u8), byteOffset, byteLength);
 }
 
 function fileURLToPath(path, options = kEmptyObject) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/lib/path.js node-v22.23.1/lib/path.js
--- node-v22.23.1/lib/path.js	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/lib/path.js	2026-07-26 23:47:19.157829723 -0500
@@ -71,6 +71,14 @@
   return code === CHAR_FORWARD_SLASH;
 }
 
+function arosVolumeEnd(path) {
+  const volumeEnd = StringPrototypeIndexOf(path, ':');
+  const firstSeparator = StringPrototypeIndexOf(path, '/');
+  return volumeEnd > 0 &&
+    (firstSeparator === -1 || volumeEnd < firstSeparator) ?
+    volumeEnd : -1;
+}
+
 const WINDOWS_RESERVED_NAMES = [
   'CON', 'PRN', 'AUX', 'NUL',
   'COM1', 'COM2', 'COM3', 'COM4', 'COM5', 'COM6', 'COM7', 'COM8', 'COM9',
@@ -1266,6 +1274,7 @@
     }
     let resolvedPath = '';
     let resolvedAbsolute = false;
+    let resolvedVolume = false;
 
     for (let i = args.length - 1; i >= 0 && !resolvedAbsolute; i--) {
       const path = args[i];
@@ -1277,15 +1286,25 @@
       }
 
       resolvedPath = `${path}/${resolvedPath}`;
+      const volumeEnd = StringPrototypeIndexOf(path, ':');
+      const firstSeparator = StringPrototypeIndexOf(path, '/');
+      resolvedVolume = volumeEnd > 0 &&
+        (firstSeparator === -1 || volumeEnd < firstSeparator);
       resolvedAbsolute =
-        StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH;
+        StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH ||
+        resolvedVolume;
     }
 
     if (!resolvedAbsolute) {
       const cwd = posixCwd();
       resolvedPath = `${cwd}/${resolvedPath}`;
+      const volumeEnd = StringPrototypeIndexOf(cwd, ':');
+      const firstSeparator = StringPrototypeIndexOf(cwd, '/');
+      resolvedVolume = volumeEnd > 0 &&
+        (firstSeparator === -1 || volumeEnd < firstSeparator);
       resolvedAbsolute =
-        StringPrototypeCharCodeAt(cwd, 0) === CHAR_FORWARD_SLASH;
+        StringPrototypeCharCodeAt(cwd, 0) === CHAR_FORWARD_SLASH ||
+        resolvedVolume;
     }
 
     // At this point the path should be resolved to a full absolute path, but
@@ -1295,7 +1314,7 @@
     resolvedPath = normalizeString(resolvedPath, !resolvedAbsolute, '/',
                                    isPosixPathSeparator);
 
-    if (resolvedAbsolute) {
+    if (resolvedAbsolute && !resolvedVolume) {
       return `/${resolvedPath}`;
     }
     return resolvedPath.length > 0 ? resolvedPath : '.';
@@ -1311,8 +1330,10 @@
     if (path.length === 0)
       return '.';
 
+    const volumeEnd = arosVolumeEnd(path);
+    const isVolume = volumeEnd !== -1;
     const isAbsolute =
-      StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH;
+      StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH || isVolume;
     const trailingSeparator =
       StringPrototypeCharCodeAt(path, path.length - 1) === CHAR_FORWARD_SLASH;
 
@@ -1327,7 +1348,7 @@
     if (trailingSeparator)
       path += '/';
 
-    return isAbsolute ? `/${path}` : path;
+    return isAbsolute && !isVolume ? `/${path}` : path;
   },
 
   /**
@@ -1336,8 +1357,10 @@
    */
   isAbsolute(path) {
     validateString(path, 'path');
+    const volumeEnd = arosVolumeEnd(path);
     return path.length > 0 &&
-           StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH;
+      (StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH ||
+       volumeEnd !== -1);
   },
 
   /**
@@ -1382,10 +1405,21 @@
     if (from === to)
       return '';
 
-    const fromStart = 1;
+    const fromVolumeEnd = arosVolumeEnd(from);
+    const toVolumeEnd = arosVolumeEnd(to);
+    if (fromVolumeEnd !== -1 || toVolumeEnd !== -1) {
+      const fromVolume = StringPrototypeToLowerCase(
+        StringPrototypeSlice(from, 0, fromVolumeEnd + 1));
+      const toVolume = StringPrototypeToLowerCase(
+        StringPrototypeSlice(to, 0, toVolumeEnd + 1));
+      if (fromVolume !== toVolume)
+        return to;
+    }
+
+    const fromStart = fromVolumeEnd !== -1 ? fromVolumeEnd + 1 : 1;
     const fromEnd = from.length;
     const fromLen = fromEnd - fromStart;
-    const toStart = 1;
+    const toStart = toVolumeEnd !== -1 ? toVolumeEnd + 1 : 1;
     const toLen = to.length - toStart;
 
     // Compare paths to find the longest common path from root
@@ -1457,10 +1491,14 @@
     validateString(path, 'path');
     if (path.length === 0)
       return '.';
-    const hasRoot = StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH;
+    const volumeEnd = arosVolumeEnd(path);
+    const hasVolume = volumeEnd !== -1;
+    const hasRoot =
+      StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH || hasVolume;
+    const rootEnd = hasVolume ? volumeEnd + 1 : 1;
     let end = -1;
     let matchedSlash = true;
-    for (let i = path.length - 1; i >= 1; --i) {
+    for (let i = path.length - 1; i >= rootEnd; --i) {
       if (StringPrototypeCharCodeAt(path, i) === CHAR_FORWARD_SLASH) {
         if (!matchedSlash) {
           end = i;
@@ -1472,9 +1510,12 @@
       }
     }
 
-    if (end === -1)
+    if (end === -1) {
+      if (hasVolume)
+        return StringPrototypeSlice(path, 0, rootEnd);
       return hasRoot ? '/' : '.';
-    if (hasRoot && end === 1)
+    }
+    if (!hasVolume && hasRoot && end === 1)
       return '//';
     return StringPrototypeSlice(path, 0, end);
   },
@@ -1488,6 +1529,9 @@
     if (suffix !== undefined)
       validateString(suffix, 'suffix');
     validateString(path, 'path');
+    const volumeEnd = arosVolumeEnd(path);
+    if (volumeEnd !== -1)
+      path = StringPrototypeSlice(path, volumeEnd + 1);
 
     let start = 0;
     let end = -1;
@@ -1565,6 +1609,9 @@
    */
   extname(path) {
     validateString(path, 'path');
+    const volumeEnd = arosVolumeEnd(path);
+    if (volumeEnd !== -1)
+      path = StringPrototypeSlice(path, volumeEnd + 1);
     let startDot = -1;
     let startPart = 0;
     let end = -1;
@@ -1633,10 +1680,15 @@
     const ret = { root: '', dir: '', base: '', ext: '', name: '' };
     if (path.length === 0)
       return ret;
+    const volumeEnd = arosVolumeEnd(path);
+    const hasVolume = volumeEnd !== -1;
     const isAbsolute =
-      StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH;
+      StringPrototypeCharCodeAt(path, 0) === CHAR_FORWARD_SLASH || hasVolume;
     let start;
-    if (isAbsolute) {
+    if (hasVolume) {
+      ret.root = StringPrototypeSlice(path, 0, volumeEnd + 1);
+      start = volumeEnd + 1;
+    } else if (isAbsolute) {
       ret.root = '/';
       start = 1;
     } else {
@@ -1684,7 +1736,8 @@
     }
 
     if (end !== -1) {
-      const start = startPart === 0 && isAbsolute ? 1 : startPart;
+      const componentStart =
+        startPart === 0 && isAbsolute ? start : startPart;
       if (startDot === -1 ||
           // We saw a non-dot character immediately before the dot
           preDotState === 0 ||
@@ -1692,10 +1745,11 @@
           (preDotState === 1 &&
           startDot === end - 1 &&
           startDot === startPart + 1)) {
-        ret.base = ret.name = StringPrototypeSlice(path, start, end);
+        ret.base = ret.name =
+          StringPrototypeSlice(path, componentStart, end);
       } else {
-        ret.name = StringPrototypeSlice(path, start, startDot);
-        ret.base = StringPrototypeSlice(path, start, end);
+        ret.name = StringPrototypeSlice(path, componentStart, startDot);
+        ret.base = StringPrototypeSlice(path, componentStart, end);
         ret.ext = StringPrototypeSlice(path, startDot, end);
       }
     }
@@ -1703,7 +1757,7 @@
     if (startPart > 0)
       ret.dir = StringPrototypeSlice(path, 0, startPart - 1);
     else if (isAbsolute)
-      ret.dir = '/';
+      ret.dir = ret.root;
 
     return ret;
   },
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/node.gyp node-v22.23.1/node.gyp
--- node-v22.23.1/node.gyp	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/node.gyp	2026-07-26 12:23:53.877007420 -0500
@@ -598,7 +598,8 @@
             }],
           ],
         }],
-        [ 'node_intermediate_lib_type=="static_library" and node_shared=="false"', {
+        [ 'node_intermediate_lib_type=="static_library" and node_shared=="false" '
+            'and node_use_bundled_v8=="true"', {
           'xcode_settings': {
             'OTHER_LDFLAGS': [
               '-Wl,-force_load,<(PRODUCT_DIR)/<(STATIC_LIB_PREFIX)<(node_core_target_name)<(STATIC_LIB_SUFFIX)',
@@ -614,6 +615,15 @@
             },
           },
           'conditions': [
+            ['node_use_node_v8_library=="true"', {
+              'libraries!': [
+                '-lv8',
+              ],
+              'libraries': [
+                '-lnode-v8',
+                '-lv8',
+              ],
+            }],
             ['OS != "aix" and OS != "os400" and OS != "mac" and OS != "ios"', {
               'ldflags': [
                 '-Wl,--whole-archive',
@@ -627,6 +637,38 @@
             }],
           ],
         }],
+        [ 'node_intermediate_lib_type=="static_library" and node_shared=="false" '
+            'and node_use_bundled_v8=="false"', {
+          'libraries': [
+            '<@(node_aros_libraries)',
+            '<@(node_v8_libraries)',
+            '-lv8',
+          ],
+          'xcode_settings': {
+            'OTHER_LDFLAGS': [
+              '-Wl,-force_load,<(PRODUCT_DIR)/<(STATIC_LIB_PREFIX)<(node_core_target_name)<(STATIC_LIB_SUFFIX)',
+            ],
+          },
+          'msvs_settings': {
+            'VCLinkerTool': {
+              'AdditionalOptions': [
+                '/WHOLEARCHIVE:<(PRODUCT_DIR)/lib/<(node_lib_target_name)<(STATIC_LIB_SUFFIX)',
+              ],
+            },
+          },
+          'conditions': [
+            ['OS != "aix" and OS != "os400" and OS != "mac" and OS != "ios"', {
+              'ldflags': [
+                '-Wl,--whole-archive',
+                '<(obj_dir)/<(STATIC_LIB_PREFIX)<(node_core_target_name)<(STATIC_LIB_SUFFIX)',
+                '-Wl,--no-whole-archive',
+              ],
+            }],
+            [ 'OS=="win"', {
+              'sources': [ 'src/res/node.rc' ],
+            }],
+          ],
+        }],
         [ 'node_shared=="true"', {
           'xcode_settings': {
             'OTHER_LDFLAGS': [ '-Wl,-rpath,@loader_path', '-Wl,-rpath,@loader_path/../lib'],
@@ -837,6 +879,7 @@
 
       'include_dirs': [
         'src',
+        'deps/v8/include',
         'deps/postject',
         '<(SHARED_INTERMEDIATE_DIR)' # for node_natives.h
       ],
@@ -1356,6 +1399,10 @@
       'include_dirs': [
         'tools',
         'src',
+        'deps/uv/include',
+      ],
+      'dependencies': [
+        'deps/uv/uv.gyp:libuv#host',
       ],
       'sources': [
         'tools/js2c.cc',
@@ -1367,9 +1414,6 @@
         [ 'node_shared_simdutf=="false"', {
           'dependencies': [ 'deps/simdutf/simdutf.gyp:simdutf#host' ],
         }],
-        [ 'node_shared_libuv=="false"', {
-          'dependencies': [ 'deps/uv/uv.gyp:libuv#host' ],
-        }],
         [ 'OS in "linux mac openharmony"', {
           'defines': ['NODE_JS2C_USE_STRING_LITERALS'],
         }],
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/node.gypi node-v22.23.1/node.gypi
--- node-v22.23.1/node.gypi	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/node.gypi	2026-07-26 12:19:29.448409896 -0500
@@ -313,8 +313,9 @@
         'NODE_PLATFORM="sunos"',
       ],
     }],
-    [ '(OS=="freebsd" or OS=="linux" or OS=="openharmony") and node_shared=="false"'
-        ' and force_load=="true"', {
+    [ 'node_use_bundled_v8=="true" and '
+        '(OS=="freebsd" or OS=="linux" or OS=="openharmony") and '
+        'node_shared=="false" and force_load=="true"', {
       'ldflags': [
         '-Wl,-z,noexecstack',
         '-Wl,--whole-archive <(v8_base)',
@@ -389,9 +390,6 @@
           ],
           'dependencies': [
             './deps/openssl/openssl.gyp:openssl',
-
-            # For tests
-            './deps/openssl/openssl.gyp:openssl-cli',
           ],
           'conditions': [
             # -force_load or --whole-archive are not applicable for
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/api/environment.cc node-v22.23.1/src/api/environment.cc
--- node-v22.23.1/src/api/environment.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/api/environment.cc	2026-08-10 06:01:42.519989251 -0500
@@ -1,4 +1,5 @@
 #include <cstdlib>
+#include <cstring>
 #if HAVE_OPENSSL
 #include "crypto/crypto_util.h"
 #endif  // HAVE_OPENSSL
@@ -111,10 +112,18 @@
   void* ret;
   if (zero_fill_field_ || per_process::cli_options->zero_fill_all_buffers) {
     COUNT_GENERIC_USAGE("NodeArrayBufferAllocator.Allocate.ZeroFilled");
+#if defined(__AROS__)
+    ret = std::calloc(1, size);
+#else
     ret = allocator_->Allocate(size);
+#endif
   } else {
     COUNT_GENERIC_USAGE("NodeArrayBufferAllocator.Allocate.Uninitialized");
+#if defined(__AROS__)
+    ret = std::malloc(size);
+#else
     ret = allocator_->AllocateUninitialized(size);
+#endif
   }
   if (ret != nullptr) [[likely]] {
     total_mem_usage_.fetch_add(size, std::memory_order_relaxed);
@@ -124,7 +133,11 @@
 
 void* NodeArrayBufferAllocator::AllocateUninitialized(size_t size) {
   COUNT_GENERIC_USAGE("NodeArrayBufferAllocator.Allocate.Uninitialized");
+#if defined(__AROS__)
+  void* ret = std::malloc(size);
+#else
   void* ret = allocator_->AllocateUninitialized(size);
+#endif
   if (ret != nullptr) [[likely]] {
     total_mem_usage_.fetch_add(size, std::memory_order_relaxed);
   }
@@ -133,7 +146,11 @@
 
 void NodeArrayBufferAllocator::Free(void* data, size_t size) {
   total_mem_usage_.fetch_sub(size, std::memory_order_relaxed);
+#if defined(__AROS__)
+  std::free(data);
+#else
   allocator_->Free(data, size);
+#endif
 }
 
 DebuggingArrayBufferAllocator::~DebuggingArrayBufferAllocator() {
@@ -200,10 +217,18 @@
 }
 
 ArrayBufferAllocator* CreateArrayBufferAllocator() {
+#if defined(__AROS__)
+  static NodeArrayBufferAllocator AROSOwnedArrayBufferAllocator;
+  return &AROSOwnedArrayBufferAllocator;
+#else
   return ArrayBufferAllocator::Create().release();
+#endif
 }
 
 void FreeArrayBufferAllocator(ArrayBufferAllocator* allocator) {
+#if defined(__AROS__)
+  return;
+#endif
   delete allocator;
 }
 
@@ -493,7 +518,14 @@
   }
 #endif
 
-  if (!use_snapshot && env->principal_realm()->RunBootstrapping().IsEmpty()) {
+  PrincipalRealm* principal_realm = env->principal_realm();
+#if defined(__AROS__)
+  fprintf(stderr,
+          "[NodeEnv:AROS] CreateEnvironment principal_realm=%p use_snapshot=%d\n",
+          principal_realm,
+          use_snapshot ? 1 : 0);
+#endif
+  if (!use_snapshot && principal_realm->RunBootstrapping().IsEmpty()) {
     FreeEnvironment(env);
     return nullptr;
   }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/async_wrap.cc node-v22.23.1/src/async_wrap.cc
--- node-v22.23.1/src/async_wrap.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/async_wrap.cc	2026-09-13 00:25:30.074493545 -0500
@@ -25,11 +25,36 @@
 #include "env-inl.h"
 #include "node_errors.h"
 #include "node_external_reference.h"
+#include "node_realm-inl.h"
 #include "tracing/traced_value.h"
 #include "util-inl.h"
 
 #include "v8.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 using v8::Context;
 using v8::DontDelete;
 using v8::EscapableHandleScope;
@@ -65,8 +90,45 @@
 #undef V
 };
 
+#ifdef __AROS__
+static Realm* ArosCurrentRealm(Environment* env, const char* where) {
+  Realm* realm = Realm::GetCurrent(env->isolate());
+  kprintf("[AsyncWrap:AROS] %s env=%p realm=%p\n", where, env, realm);
+  return realm;
+}
+
+static Local<Function> ArosAsyncHookFunction(Environment* env,
+                                            AsyncHooks::Fields type) {
+  Realm* realm = ArosCurrentRealm(env, "hook lookup");
+  if (realm == nullptr) return Local<Function>();
+
+  switch (type) {
+    case AsyncHooks::kInit:
+      return realm->async_hooks_init_function();
+    case AsyncHooks::kBefore:
+      return realm->async_hooks_before_function();
+    case AsyncHooks::kAfter:
+      return realm->async_hooks_after_function();
+    case AsyncHooks::kDestroy:
+      return realm->async_hooks_destroy_function();
+    case AsyncHooks::kPromiseResolve:
+      return realm->async_hooks_promise_resolve_function();
+    default:
+      return Local<Function>();
+  }
+}
+#endif
+
 void AsyncWrap::DestroyAsyncIdsCallback(Environment* env) {
+#ifdef __AROS__
+  Local<Function> fn = ArosAsyncHookFunction(env, AsyncHooks::kDestroy);
+  if (fn.IsEmpty()) {
+    kprintf("[AsyncWrap:AROS] destroy hook missing; skipping queued destroy callbacks\n");
+    return;
+  }
+#else
   Local<Function> fn = env->async_hooks_destroy_function();
+#endif
 
   TryCatchScope try_catch(env,
                           TryCatchScope::CatchMode::kFatalRethrowStackOverflow);
@@ -96,6 +158,14 @@
   if (async_hooks->fields()[type] == 0 || !env->can_call_into_js())
     return;
 
+#ifdef __AROS__
+  if (fn.IsEmpty()) {
+    kprintf("[AsyncWrap:AROS] requested hook type=%d but function is empty\n",
+            static_cast<int>(type));
+    return;
+  }
+#endif
+
   HandleScope handle_scope(env->isolate());
   Local<Value> async_id_value = Number::New(env->isolate(), async_id);
   TryCatchScope try_catch(env,
@@ -105,8 +175,13 @@
 
 
 void AsyncWrap::EmitPromiseResolve(Environment* env, double async_id) {
+#ifdef __AROS__
+  Emit(env, async_id, AsyncHooks::kPromiseResolve,
+       ArosAsyncHookFunction(env, AsyncHooks::kPromiseResolve));
+#else
   Emit(env, async_id, AsyncHooks::kPromiseResolve,
        env->async_hooks_promise_resolve_function());
+#endif
 }
 
 void AsyncWrap::EmitTraceAsyncStart() const {
@@ -139,8 +214,13 @@
 
 
 void AsyncWrap::EmitBefore(Environment* env, double async_id) {
+#ifdef __AROS__
+  Emit(env, async_id, AsyncHooks::kBefore,
+       ArosAsyncHookFunction(env, AsyncHooks::kBefore));
+#else
   Emit(env, async_id, AsyncHooks::kBefore,
        env->async_hooks_before_function());
+#endif
 }
 
 
@@ -163,12 +243,25 @@
 void AsyncWrap::EmitAfter(Environment* env, double async_id) {
   // If the user's callback failed then the after() hooks will be called at the
   // end of _fatalException().
+#ifdef __AROS__
+  Emit(env, async_id, AsyncHooks::kAfter,
+       ArosAsyncHookFunction(env, AsyncHooks::kAfter));
+#else
   Emit(env, async_id, AsyncHooks::kAfter,
        env->async_hooks_after_function());
+#endif
 }
 
 static void SetupHooks(const FunctionCallbackInfo<Value>& args) {
   Environment* env = Environment::GetCurrent(args);
+#ifdef __AROS__
+  Realm* realm = Realm::GetCurrent(args);
+  kprintf("[AsyncWrap:AROS] SetupHooks env=%p realm=%p argc=%d\n",
+          env,
+          realm,
+          args.Length());
+  CHECK_NOT_NULL(realm);
+#endif
 
   CHECK(args[0]->IsObject());
 
@@ -176,10 +269,25 @@
   // async_hooks internally, so this should only ever be called once. At which
   // time all the functions should be set. Detect this by checking if
   // init !IsEmpty().
+#ifdef __AROS__
+  CHECK(realm->async_hooks_init_function().IsEmpty());
+#else
   CHECK(env->async_hooks_init_function().IsEmpty());
+#endif
 
   Local<Object> fn_obj = args[0].As<Object>();
 
+#ifdef __AROS__
+#define SET_HOOK_FN(name)                                                      \
+  do {                                                                         \
+    Local<Value> v =                                                           \
+        fn_obj->Get(env->context(),                                            \
+                    FIXED_ONE_BYTE_STRING(env->isolate(), #name))              \
+            .ToLocalChecked();                                                 \
+    CHECK(v->IsFunction());                                                    \
+    realm->set_async_hooks_##name##_function(v.As<Function>());                \
+  } while (0)
+#else
 #define SET_HOOK_FN(name)                                                      \
   do {                                                                         \
     Local<Value> v =                                                           \
@@ -189,6 +297,7 @@
     CHECK(v->IsFunction());                                                    \
     env->set_async_hooks_##name##_function(v.As<Function>());                  \
   } while (0)
+#endif
 
   SET_HOOK_FN(init);
   SET_HOOK_FN(before);
@@ -349,11 +458,25 @@
 void AsyncWrap::SetCallbackTrampoline(const FunctionCallbackInfo<Value>& args) {
   Environment* env = Environment::GetCurrent(args);
 
+#ifdef __AROS__
+  Realm* realm = Realm::GetCurrent(args);
+  kprintf("[AsyncWrap:AROS] SetCallbackTrampoline env=%p realm=%p arg0_is_function=%d\n",
+          env,
+          realm,
+          args.Length() > 0 && args[0]->IsFunction());
+  CHECK_NOT_NULL(realm);
+  if (args[0]->IsFunction()) {
+    realm->set_async_hooks_callback_trampoline(args[0].As<Function>());
+  } else {
+    realm->set_async_hooks_callback_trampoline(Local<Function>());
+  }
+#else
   if (args[0]->IsFunction()) {
     env->set_async_hooks_callback_trampoline(args[0].As<Function>());
   } else {
     env->set_async_hooks_callback_trampoline(Local<Function>());
   }
+#endif
 }
 
 Local<FunctionTemplate> AsyncWrap::GetConstructorTemplate(
@@ -639,7 +762,15 @@
   }
 
   HandleScope scope(env->isolate());
+#ifdef __AROS__
+  Local<Function> init_fn = ArosAsyncHookFunction(env, AsyncHooks::kInit);
+  if (init_fn.IsEmpty()) {
+    kprintf("[AsyncWrap:AROS] init hook requested but function is empty\n");
+    return;
+  }
+#else
   Local<Function> init_fn = env->async_hooks_init_function();
+#endif
 
   Local<Value> argv[] = {
     Number::New(env->isolate(), async_id),
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/crypto/crypto_context.cc node-v22.23.1/src/crypto/crypto_context.cc
--- node-v22.23.1/src/crypto/crypto_context.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/crypto/crypto_context.cc	2026-09-15 13:22:58.733440187 -0500
@@ -997,6 +997,30 @@
 }
 
 void CleanupCachedRootCertificates() {
+#ifdef __AROS__
+  /* The three caches below are function-local statics, i.e. they live in
+     node.library and are shared by EVERY opener - upstream can free them here
+     because one process owns the whole node runtime and exits right after.
+
+     On AROS each C:Node is a Process of its own while node.library stays
+     loaded: the vectors hold one reference per certificate on behalf of the
+     LIBRARY, and every live opener's X509_STORE holds references into the very
+     same X509 objects (NewRootCertStore -> X509_STORE_add_cert).  Freeing the
+     library's references when one Process exits therefore pulls the roots out
+     from under the others, and a second exiting Process frees them again -
+     the vector is never cleared and has_cached_* never resets.  Desktop VS
+     Code run 27: clicking "Sign in" forked a short-lived C:Node for the OAuth
+     hand-off, and its teardown faulted in ossl_asn1_string_embed_free with a
+     poisoned ASN1_STRING pointer (0xD0 over the high byte) while the Electron
+     main process was still running with the same roots.
+
+     So the library keeps them for its whole life, exactly as the V8 process
+     state above is kept (see the __AROS__ note in node.cc's
+     TearDownOncePerProcess): they are immutable data derived from a compiled-in
+     table, one instance per library load, reclaimed when node.library expunges
+     and its memory goes with it.  The thread join below still runs, because
+     that thread belongs to this invocation. */
+#else
   if (has_cached_bundled_root_certs.load()) {
     for (X509* cert : GetBundledRootCertificates()) {
       X509_free(cert);
@@ -1013,6 +1037,7 @@
       X509_free(cert);
     }
   }
+#endif  // __AROS__ : the library owns these references for its whole life
 
   // Serialize with starter to avoid the race window.
   Mutex::ScopedLock lock(start_cert_loading_thread_mutex);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/debug_utils.cc node-v22.23.1/src/debug_utils.cc
--- node-v22.23.1/src/debug_utils.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/debug_utils.cc	2026-07-25 20:05:15.111399183 -0500
@@ -12,7 +12,8 @@
 #include <android/log.h>
 #endif
 
-#if defined(__linux__) && !defined(__GLIBC__) || \
+#if defined(__AROS__) || \
+    defined(__linux__) && !defined(__GLIBC__) || \
     defined(__UCLIBC__) || \
     defined(_AIX)
 #define HAVE_EXECINFO_H 0
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/env.cc node-v22.23.1/src/env.cc
--- node-v22.23.1/src/env.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/env.cc	2026-08-10 06:01:35.025051922 -0500
@@ -985,8 +985,19 @@
 
 void Environment::InitializeMainContext(Local<Context> context,
                                         const EnvSerializeInfo* env_info) {
+#if defined(__AROS__)
+  fprintf(stderr,
+          "[NodeEnv:AROS] InitializeMainContext begin env=%p env_info=%p\n",
+          this,
+          env_info);
+#endif
   principal_realm_ = std::make_unique<PrincipalRealm>(
       this, context, MAYBE_FIELD_PTR(env_info, principal_realm));
+#if defined(__AROS__)
+  fprintf(stderr,
+          "[NodeEnv:AROS] InitializeMainContext realm=%p\n",
+          principal_realm_.get());
+#endif
   if (env_info != nullptr) {
     DeserializeProperties(env_info);
   }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/env-inl.h node-v22.23.1/src/env-inl.h
--- node-v22.23.1/src/env-inl.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/env-inl.h	2026-08-10 07:09:38.046884396 -0500
@@ -907,6 +907,31 @@
   PER_ISOLATE_TEMPLATE_PROPERTIES(V)
 #undef V
 
+#ifdef __AROS__
+#define V(PropertyName, TypeName)                                              \
+  inline v8::Local<TypeName> Environment::PropertyName() const {               \
+    Realm* realm = principal_realm_.get();                                     \
+    DCHECK_NOT_NULL(realm);                                                    \
+    if (realm == nullptr) [[unlikely]] {                                       \
+      realm = Realm::GetCurrent(isolate());                                    \
+    }                                                                          \
+    if (realm == nullptr) [[unlikely]] {                                       \
+      return v8::Local<TypeName>();                                            \
+    }                                                                          \
+    return realm->PropertyName();                                              \
+  }                                                                            \
+  inline void Environment::set_##PropertyName(v8::Local<TypeName> value) {     \
+    Realm* realm = principal_realm_.get();                                     \
+    DCHECK_NOT_NULL(realm);                                                    \
+    if (realm == nullptr) [[unlikely]] {                                       \
+      realm = Realm::GetCurrent(isolate());                                    \
+    }                                                                          \
+    if (realm == nullptr) [[unlikely]] {                                       \
+      return;                                                                  \
+    }                                                                          \
+    realm->set_##PropertyName(value);                                          \
+  }
+#else
 #define V(PropertyName, TypeName)                                              \
   inline v8::Local<TypeName> Environment::PropertyName() const {               \
     DCHECK_NOT_NULL(principal_realm_);                                         \
@@ -916,7 +941,8 @@
     DCHECK_NOT_NULL(principal_realm_);                                         \
     principal_realm_->set_##PropertyName(value);                               \
   }
-  PER_REALM_STRONG_PERSISTENT_VALUES(V)
+#endif
+PER_REALM_STRONG_PERSISTENT_VALUES(V)
 #undef V
 
 v8::Local<v8::Context> Environment::context() const {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/large_pages/node_large_page.cc node-v22.23.1/src/large_pages/node_large_page.cc
--- node-v22.23.1/src/large_pages/node_large_page.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/large_pages/node_large_page.cc	2026-07-25 21:09:16.494325095 -0500
@@ -60,7 +60,8 @@
 #include <cerrno>   // NOLINT(build/include)
 
 // Besides returning ENOTSUP at runtime we do nothing if this define is missing.
-#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES
+#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES && \
+    !defined(__AROS__)
 #include "debug_utils-inl.h"
 
 #if defined(__linux__) || defined(__FreeBSD__)
@@ -106,7 +107,8 @@
 
 #endif  // defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES
 namespace node {
-#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES
+#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES && \
+    !defined(__AROS__)
 
 namespace {
 
@@ -415,7 +417,8 @@
 
 // This is the primary API called from main.
 int MapStaticCodeToLargePages() {
-#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES
+#if defined(NODE_ENABLE_LARGE_CODE_PAGES) && NODE_ENABLE_LARGE_CODE_PAGES && \
+    !defined(__AROS__)
   bool have_thp = false;
 #if defined(__linux__)
   have_thp = IsTransparentHugePagesEnabled();
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/memory_tracker-inl.h node-v22.23.1/src/memory_tracker-inl.h
--- node-v22.23.1/src/memory_tracker-inl.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/memory_tracker-inl.h	2026-07-25 19:51:30.296143787 -0500
@@ -6,6 +6,10 @@
 #include "memory_tracker.h"
 #include "util-inl.h"
 
+#if defined(__AROS__) && defined(GetNodeName)
+#undef GetNodeName
+#endif
+
 namespace node {
 
 // Fallback edge_name if node_name is not available, or "" if edge_name
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_binding.cc node-v22.23.1/src/node_binding.cc
--- node-v22.23.1/src/node_binding.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_binding.cc	2026-09-13 00:25:30.075245941 -0500
@@ -8,6 +8,30 @@
 
 #include <string>
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 #if HAVE_OPENSSL
 #define NODE_BUILTIN_OPENSSL_BINDINGS(V) V(crypto) V(tls_wrap)
 #else
@@ -339,7 +363,7 @@
 DLib::DLib(const char* filename, int flags)
     : filename_(filename), flags_(flags), handle_(nullptr) {}
 
-#ifdef __POSIX__
+#if defined(__POSIX__) && !defined(__AROS__)
 bool DLib::Open() {
   handle_ = dlopen(filename_.c_str(), flags_);
   if (handle_ != nullptr) return true;
@@ -395,7 +419,7 @@
   if (0 == uv_dlsym(&lib_, name, &address)) return address;
   return nullptr;
 }
-#endif  // !__POSIX__
+#endif  // defined(__POSIX__) && !defined(__AROS__)
 
 void DLib::SaveInGlobalHandleMap(node_module* mp) {
   has_entry_in_global_handle_map_ = true;
@@ -641,11 +665,32 @@
   node::Utf8Value module_v(isolate, module);
   Local<Object> exports;
 
+#ifdef __AROS__
+  kprintf("[NodeBinding:AROS] GetInternalBinding name=%s realm=%p\n",
+          *module_v,
+          realm);
+#endif
   node_module* mod = FindModule(modlist_internal, *module_v, NM_F_INTERNAL);
   if (mod != nullptr) {
+#ifdef __AROS__
+    kprintf("[NodeBinding:AROS] InitInternalBinding enter name=%s mod=%p "
+            "ctx_func=%p priv=%p\n",
+            *module_v,
+            mod,
+            reinterpret_cast<void*>(mod->nm_context_register_func),
+            mod->nm_priv);
+#endif
     exports = InitInternalBinding(realm, mod);
+#ifdef __AROS__
+    kprintf("[NodeBinding:AROS] InitInternalBinding leave name=%s\n",
+            *module_v);
+#endif
     realm->internal_bindings.insert(mod);
   } else {
+#ifdef __AROS__
+    kprintf("[NodeBinding:AROS] GetInternalBinding missing name=%s\n",
+            *module_v);
+#endif
     return THROW_ERR_INVALID_MODULE(isolate, "No such binding: %s", *module_v);
   }
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_binding.h node-v22.23.1/src/node_binding.h
--- node-v22.23.1/src/node_binding.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_binding.h	2026-07-25 20:06:51.479822639 -0500
@@ -3,7 +3,7 @@
 
 #if defined(NODE_WANT_INTERNALS) && NODE_WANT_INTERNALS
 
-#if defined(__POSIX__)
+#if defined(__POSIX__) && !defined(__AROS__)
 #include <dlfcn.h>
 #endif
 
@@ -115,7 +115,7 @@
 
 class DLib {
  public:
-#ifdef __POSIX__
+#if defined(__POSIX__) && !defined(__AROS__)
   static const int kDefaultFlags = RTLD_LAZY;
 #else
   static const int kDefaultFlags = 0;
@@ -133,7 +133,7 @@
   const int flags_;
   std::string errmsg_;
   void* handle_;
-#ifndef __POSIX__
+#if !defined(__POSIX__) || defined(__AROS__)
   uv_lib_t lib_;
 #endif
   bool has_entry_in_global_handle_map_ = false;
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_buffer.cc node-v22.23.1/src/node_buffer.cc
--- node-v22.23.1/src/node_buffer.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_buffer.cc	2026-07-25 20:29:27.873241829 -0500
@@ -36,6 +36,10 @@
 #include "v8-fast-api-calls.h"
 #include "v8.h"
 
+#if defined(__AROS__)
+#include <proto/node-v8.h>
+#endif
+
 #include <stdint.h>
 #include <climits>
 #include <cstring>
@@ -1436,8 +1440,13 @@
       allocator == nullptr) {
     buf = ArrayBuffer::New(isolate, size);
   } else {
+#if defined(__AROS__)
+    std::unique_ptr<BackingStore> store(static_cast<BackingStore*>(
+        NodeV8NewBackingStoreForNodeLTS(isolate, size)));
+#else
     std::unique_ptr<BackingStore> store =
         ArrayBuffer::NewBackingStoreForNodeLTS(isolate, size);
+#endif
     if (!store) {
       return env->ThrowRangeError("Array buffer allocation failed");
     }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_builtins.cc node-v22.23.1/src/node_builtins.cc
--- node-v22.23.1/src/node_builtins.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_builtins.cc	2026-09-13 00:25:30.073166315 -0500
@@ -32,6 +32,36 @@
 using v8::Undefined;
 using v8::Value;
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+const BuiltinSourceMap& ArosGetBuiltinSourceMapDirect();
+MaybeLocal<String> ArosLoadBuiltinSourceDirect(Isolate* isolate, const char* id);
+
+static void ArosBuiltinTrace(const char* id, const char* step) {
+  kprintf("[NodeBuiltin:AROS] %s %s\n", id, step);
+}
+#endif
+
 BuiltinLoader::BuiltinLoader()
     : config_(GetConfig()), code_cache_(std::make_shared<BuiltinCodeCache>()) {
   LoadJavaScriptSource();
@@ -61,8 +91,13 @@
 }
 
 bool BuiltinLoader::Exists(const char* id) {
+#ifdef __AROS__
+  const BuiltinSourceMap& source = ArosGetBuiltinSourceMapDirect();
+  return source.find(id) != source.end();
+#else
   auto source = source_.read();
   return source->find(id) != source->end();
+#endif
 }
 
 bool BuiltinLoader::Add(const char* id, const UnionBytes& source) {
@@ -77,8 +112,13 @@
   Local<Context> context = env->context();
 
   Local<Object> out = Object::New(isolate);
+#ifdef __AROS__
+  const BuiltinSourceMap& source = ArosGetBuiltinSourceMapDirect();
+  for (auto const& x : source) {
+#else
   auto source = env->builtin_loader()->source_.read();
   for (auto const& x : *source) {
+#endif
     Local<String> key = OneByteString(isolate, x.first);
     if (out->Set(context, key, x.second.ToStringChecked(isolate)).IsNothing()) {
       return;
@@ -93,9 +133,15 @@
 
 std::vector<std::string_view> BuiltinLoader::GetBuiltinIds() const {
   std::vector<std::string_view> ids;
+#ifdef __AROS__
+  const BuiltinSourceMap& source = ArosGetBuiltinSourceMapDirect();
+  ids.reserve(source.size());
+  for (auto const& x : source) {
+#else
   auto source = source_.read();
   ids.reserve(source->size());
   for (auto const& x : *source) {
+#endif
     ids.emplace_back(x.first);
   }
   return ids;
@@ -153,8 +199,13 @@
         "internal/v8_prof_processor",
   };
 
+#ifdef __AROS__
+  const BuiltinSourceMap& source = ArosGetBuiltinSourceMapDirect();
+  for (auto const& x : source) {
+#else
   auto source = source_.read();
   for (auto const& x : *source) {
+#endif
     const std::string& id = x.first;
     for (auto const& prefix : prefixes) {
       if (prefix.length() > id.length()) {
@@ -167,7 +218,11 @@
     }
   }
 
+#ifdef __AROS__
+  for (auto const& x : source) {
+#else
   for (auto const& x : *source) {
+#endif
     const std::string& id = x.first;
     if (0 == builtin_categories.cannot_be_required.count(id)) {
       builtin_categories.can_be_required.emplace(id);
@@ -196,13 +251,29 @@
 
 MaybeLocal<String> BuiltinLoader::LoadBuiltinSource(Isolate* isolate,
                                                     const char* id) const {
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "LoadBuiltinSource begin");
+  MaybeLocal<String> direct = ArosLoadBuiltinSourceDirect(isolate, id);
+  ArosBuiltinTrace(id, "LoadBuiltinSource direct returned");
+  return direct;
+#endif
   auto source = source_.read();
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "LoadBuiltinSource source read");
+  kprintf("[NodeBuiltin:AROS] %s source map=%p size=%lu\n",
+          id,
+          &*source,
+          static_cast<unsigned long>(source->size()));
+#endif
 #ifndef NODE_BUILTIN_MODULES_PATH
   const auto source_it = source->find(id);
   if (source_it == source->end()) [[unlikely]] {
     fprintf(stderr, "Cannot find native builtin: \"%s\".\n", id);
     ABORT();
   }
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "LoadBuiltinSource found");
+#endif
   return source_it->second.ToStringChecked(isolate);
 #else   // !NODE_BUILTIN_MODULES_PATH
   std::string filename = OnDiskFileName(id);
@@ -279,15 +350,34 @@
   EscapableHandleScope scope(isolate);
 
   Local<String> source;
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "LookupAndCompile begin");
+#endif
   if (!LoadBuiltinSource(isolate, id).ToLocal(&source)) {
     return {};
   }
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "source local ready");
+#endif
 
   std::string filename_s = std::string("node:") + id;
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "filename string begin");
+#endif
   Local<String> filename = OneByteString(isolate, filename_s);
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "filename string ready");
+#endif
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "ScriptOrigin begin");
+#endif
   ScriptOrigin origin(filename, 0, 0, true);
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "ScriptOrigin ready");
+#endif
 
   BuiltinCodeCacheData cached_data{};
+#ifndef __AROS__
   {
     // Note: The lock here should not extend into the
     // `CompileFunction()` call below, because this function may recurse if
@@ -300,6 +390,9 @@
       cached_data = cache_it->second;
     }
   }
+#else
+  ArosBuiltinTrace(id, "code-cache bypass");
+#endif
 
   const bool has_cache = cached_data.data != nullptr;
   ScriptCompiler::CompileOptions options =
@@ -312,10 +405,16 @@
       options = ScriptCompiler::kEagerCompile;
     }
   }
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "ScriptCompiler::Source begin");
+#endif
   ScriptCompiler::Source script_source(
       source,
       origin,
       has_cache ? cached_data.AsCachedData().release() : nullptr);
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "ScriptCompiler::Source ready");
+#endif
 
   per_process::Debug(
       DebugCategory::CODE_CACHE,
@@ -324,6 +423,9 @@
       has_cache ? "with" : "without",
       options == ScriptCompiler::kEagerCompile ? "eagerly" : "lazily");
 
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "CompileFunction begin");
+#endif
   MaybeLocal<Function> maybe_fun =
       ScriptCompiler::CompileFunction(context,
                                       &script_source,
@@ -332,6 +434,9 @@
                                       0,
                                       nullptr,
                                       options);
+#ifdef __AROS__
+  ArosBuiltinTrace(id, "CompileFunction returned");
+#endif
 
   // This could fail when there are early errors in the built-in modules,
   // e.g. the syntax errors
@@ -376,7 +481,11 @@
     // This is only done when the isolate is not being serialized because
     // V8 does not support serializing code cache with an unfinalized read-only
     // space (which is what isolates pending to be serialized have).
+#ifndef __AROS__
     SaveCodeCache(id, fun);
+#else
+    ArosBuiltinTrace(id, "SaveCodeCache bypass");
+#endif
   }
 
   return scope.Escape(fun);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node.cc node-v22.23.1/src/node.cc
--- node-v22.23.1/src/node.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node.cc	2026-09-15 17:00:00.000000000 -0500
@@ -134,6 +134,22 @@
 #include <tuple>
 #include <vector>
 
+#if defined(__AROS__) && defined(NODE_AROS_TRACE)
+/* bug() reaches kprintf(), which needs the kernel-only _arosdebuglock; see the
+ * note at the kprintf declarations. Opt-in so node.library can link on pc. */
+#include <aros/debug.h>
+#define NODE_AROS_PHASE(name) bug("[Node] phase %s\n", name)
+#else
+#define NODE_AROS_PHASE(name) ((void) 0)
+#endif
+
+#ifdef __AROS__
+/* node.library's bridge (workbench/libs/node/node_bridge.cc): lend this
+ * library's per-task base to a thread that never opened it, for the global
+ * callbacks V8 makes from Blink's own threads. */
+extern "C" void __node_aros_bind_callback_task_base(void);
+#endif
+
 namespace node {
 
 using v8::Array;
@@ -883,19 +899,50 @@
     std::vector<std::string>* exec_argv,
     std::vector<std::string>* errors,
     ProcessInitializationFlags::Flags flags) {
+  NODE_AROS_PHASE("initialize-args check-once");
   // Make sure InitializeNodeWithArgs() is called only once.
+#ifdef __AROS__
+  /* node.library is a shared AROS library and outlives the process that
+     opened it, so this file-scope guard is still set from the PREVIOUS
+     C:Node invocation and CHECK() would abort the second one.  node is
+     written for one Start() per OS process; on AROS one library serves many
+     processes in sequence.  Record that we ran, without aborting. */
+  init_called.store(true);
+
+  /* The PARSED OPTIONS are process-global too, and they survive as well.
+     Option parsing mutates the existing per_process::cli_options object
+     rather than replacing it, so the previous invocation's flags are still
+     set when the next one parses. Observed: `Node --version` followed by
+     `Node -e "..."` prints the version a second time, because print_version
+     is still true from the first run - the second run's eval_string is
+     parsed correctly and then never reached.
+
+     TearDownOncePerProcess() does not cover this: it resets stdio, the
+     signal handlers and per_process::v8_initialized, but not the options and
+     not init_called above. Hand each invocation a fresh options object. */
+  {
+    Mutex::ScopedLock lock(per_process::cli_options_mutex);
+    per_process::cli_options = std::make_shared<PerProcessOptions>();
+  }
+#else
   CHECK(!init_called.exchange(true));
+#endif
 
   // Initialize node_start_time to get relative uptime.
+  NODE_AROS_PHASE("initialize-args hrtime");
   per_process::node_start_time = uv_hrtime();
 
   // Register built-in bindings
+  NODE_AROS_PHASE("initialize-args register-bindings enter");
   binding::RegisterBuiltinBindings();
+  NODE_AROS_PHASE("initialize-args register-bindings leave");
 
   // Make inherited handles noninheritable.
   if (!(flags & ProcessInitializationFlags::kEnableStdioInheritance) &&
       !(flags & ProcessInitializationFlags::kNoStdioInitialization)) {
+    NODE_AROS_PHASE("initialize-args disable-stdio-inheritance enter");
     uv_disable_stdio_inheritance();
+    NODE_AROS_PHASE("initialize-args disable-stdio-inheritance leave");
   }
 
   // Cache the original command line to be
@@ -905,8 +952,37 @@
   // Node provides a "v8.setFlagsFromString" method to dynamically change flags.
   // Hence do not freeze flags when initializing V8. In a browser setting, this
   // is security relevant, for Node it's less important.
+  NODE_AROS_PHASE("initialize-args v8-flags enter");
   V8::SetFlagsFromString("--no-freeze-flags-after-init");
 
+#ifdef __AROS__
+  // AROS has no lazily-committed address space: the platform mmap()
+  // (ArosMmap, deps/v8/src/base/platform/platform-posix.cc) backs every
+  // reservation with AllocMem(MEMF_CLEAR).  V8 sizes a wasm memory's
+  // backing store by its *maximum* (WasmMemoryObject::New: the engine
+  // maximum, 4 GiB, when the module declares none), so undici's llhttp --
+  // i.e. the first fetch() -- would commit and zero 4 GiB.  Cap the engine
+  // maximum at 128 MiB (2048 x 64 KiB pages); a --wasm-max-mem-pages on
+  // the command line still overrides this (SetFlagsFromCommandLine runs
+  // later).
+  V8::SetFlagsFromString("--wasm-max-mem-pages=2048");
+
+  // The AROS NodePlatform is foreground-only (node_platform.cc: no worker
+  // threads; CallOnWorkerThread runs the task inline on the posting
+  // thread), so every "background" job V8 posts -- wasm tier-up, concurrent
+  // recompilation, concurrent marking/sweeping -- executes synchronously at
+  // the point it was posted, inside whatever callback happened to trigger
+  // it (llhttp's tier-up ran for seconds inside a TLS read callback and
+  // stalled the response).  --single-threaded is V8's contract for exactly
+  // this platform shape: it stops V8 posting background tasks at all and
+  // does the work where V8 expects it to be synchronous.  It does not
+  // cover budget-driven wasm tier-up (--wasm-dynamic-tiering), which still
+  // compiles one function with TurboFan inline when its budget runs out;
+  // that is bounded by a single function's compile and lives with a
+  // release V8 -- the seconds came from a DCHECK build's verifiers.
+  V8::SetFlagsFromString("--single-threaded");
+#endif
+
 #if defined(NODE_V8_OPTIONS)
   // Should come before the call to V8::SetFlagsFromCommandLine()
   // so the user can disable a flag --foo at run-time by passing
@@ -923,6 +999,7 @@
   // Specify this explicitly to avoid being affected by V8 changes to the
   // default value.
   V8::SetFlagsFromString("--rehash-snapshot");
+  NODE_AROS_PHASE("initialize-args v8-flags leave");
 
 #if HAVE_OPENSSL
   // TODO(joyeecheung): make this a per-env option and move the normalization
@@ -933,10 +1010,14 @@
     per_process::cli_options->use_system_ca = true;
   }
 #endif  // HAVE_OPENSSL
+  NODE_AROS_PHASE("initialize-args env-options enter");
   HandleEnvOptions(per_process::cli_options->per_isolate->per_env);
+  NODE_AROS_PHASE("initialize-args env-options leave");
 
   std::string node_options;
+  NODE_AROS_PHASE("initialize-args dotenv-args enter");
   auto env_files = node::Dotenv::GetDataFromArgs(*argv);
+  NODE_AROS_PHASE("initialize-args dotenv-args leave");
 
   if (!env_files.empty()) {
     CHECK(!per_process::v8_initialized);
@@ -1035,8 +1116,10 @@
 #endif
 
   if (!(flags & ProcessInitializationFlags::kDisableCLIOptions)) {
+    NODE_AROS_PHASE("initialize-args global-args enter");
     const ExitCode exit_code =
         ProcessGlobalArgsInternal(argv, exec_argv, errors, kDisallowedInEnvvar);
+    NODE_AROS_PHASE("initialize-args global-args leave");
     if (exit_code != ExitCode::kNoFailure) return exit_code;
   }
 
@@ -1112,6 +1195,7 @@
 InitializeOncePerProcessInternal(const std::vector<std::string>& args,
                                  ProcessInitializationFlags::Flags flags =
                                      ProcessInitializationFlags::kNoFlags) {
+  NODE_AROS_PHASE("initialize-once enter");
   auto result = std::make_shared<InitializationResultImpl>();
   result->args_ = args;
 
@@ -1121,12 +1205,16 @@
     per_process::enabled_debug_list.Parse(nullptr);
   }
 
+  NODE_AROS_PHASE("platform-init enter");
   PlatformInit(flags);
+  NODE_AROS_PHASE("platform-init leave");
 
   // This needs to run *before* V8::Initialize().
   {
+    NODE_AROS_PHASE("initialize-args enter");
     result->exit_code_ = InitializeNodeWithArgsInternal(
         &result->args_, &result->exec_args_, &result->errors_, flags);
+    NODE_AROS_PHASE("initialize-args leave");
     if (result->exit_code_enum() != ExitCode::kNoFailure) {
       result->early_return_ = true;
       return result;
@@ -1266,6 +1354,17 @@
     CHECK(ncrypto::CSPRNG(nullptr, 0));
 
     V8::SetEntropySource([](unsigned char* buffer, size_t length) {
+#ifdef __AROS__
+      /* V8 calls this on whatever thread builds an Isolate, and in an
+         Electron process that includes threads Blink creates: a service
+         worker's Isolate::Init -> Heap::SetUpSpaces -> MemoryMeasurement ->
+         v8::base::RandomNumberGenerator lands here, inside node.library, on
+         a task that never opened it.  Bind this library's per-task base
+         before the CSPRNG call reaches a rel-wrapper with none (desktop VS
+         Code gate 11, 2026-09-15: __strcmp_StdCBase_relwrapper faulting at
+         the StdCBase offset under ossl_drbg_get_ctx_params_no_lock). */
+      __node_aros_bind_callback_task_base();
+#endif
       // V8 falls back to very weak entropy when this function fails
       // and /dev/urandom isn't available. That wouldn't be so bad if
       // the entropy was only used for Math.random() but it's also used for
@@ -1282,14 +1381,63 @@
 #endif  // HAVE_OPENSSL
   }
 
+  /* AROS: the V8 platform, V8 itself and cppgc's process heap are
+     PROCESS-GLOBAL and ONE-WAY. Upstream disposes them from
+     TearDownOncePerProcess() on the way to process exit, and V8 does not
+     support V8::Initialize() after V8::Dispose(). node.library outlives the
+     AROS process that opened it, so on a second C:Node these are already up;
+     re-initializing them faulted reading NULL+8 (CR2=0x8, at the
+     "v8-platform enter" phase, right after the previous run's
+     V8::DisposePlatform()).
+
+     So: initialize them exactly once per library load and reuse them after
+     that. per_process::v8_initialized is the existing flag for "these are
+     up"; the matching half of this fix is that TearDownOncePerProcess() must
+     not dispose them and must not clear that flag on AROS.
+
+     result->platform_ is still assigned on every invocation - the caller
+     needs the pointer whether or not we created it this time round.
+
+     The platform is guarded by ITS OWN one-way state, not by
+     per_process::v8_initialized: an embedder (node.library's
+     NodePrepareEmbedder for Electron) initializes with
+     kNoInitializeNodeV8Platform, owns V8 through its own MultiIsolatePlatform
+     and still leaves v8_initialized set below.  A C:Node started while (or
+     after) that Electron process holds the library - the VS Code extension
+     host spawning the Claude Code CLI - then found per_process::v8_platform
+     never initialized, Platform() == NULL, and NewIsolate() faulted on the
+     NULL platform (2026-09-15).
+
+     v8.library's platform pointer is task-local (parent-slot inherited), so
+     a kept platform must still be installed on each new invocation's task:
+     on AROS V8::InitializePlatform() only records the slot once V8 is up.
+     Without this a task with no slot falls back to the engine's first-ever
+     platform, which belongs to whichever process installed it. */
   if (!(flags & ProcessInitializationFlags::kNoInitializeNodeV8Platform)) {
+    NODE_AROS_PHASE("v8-platform enter");
+#ifdef __AROS__
+    if (!per_process::v8_platform.initialized_) {
+      per_process::v8_platform.Initialize(
+          static_cast<int>(per_process::cli_options->v8_thread_pool_size));
+    } else {
+      V8::InitializePlatform(per_process::v8_platform.Platform());
+    }
+#else
     per_process::v8_platform.Initialize(
         static_cast<int>(per_process::cli_options->v8_thread_pool_size));
+#endif
     result->platform_ = per_process::v8_platform.Platform();
+    NODE_AROS_PHASE("v8-platform leave");
   }
 
-  if (!(flags & ProcessInitializationFlags::kNoInitializeV8)) {
+  if (!(flags & ProcessInitializationFlags::kNoInitializeV8)
+#ifdef __AROS__
+      && !per_process::v8_initialized
+#endif
+  ) {
+    NODE_AROS_PHASE("v8-initialize enter");
     V8::Initialize();
+    NODE_AROS_PHASE("v8-initialize leave");
 
     // Disable absl deadlock detection in V8 as it reports false-positive cases.
     // TODO(legendecas): Replace this global disablement with case suppressions.
@@ -1297,12 +1445,18 @@
     absl::SetMutexDeadlockDetectionMode(absl::OnDeadlockCycle::kIgnore);
   }
 
-  if (!(flags & ProcessInitializationFlags::kNoInitializeCppgc)) {
+  if (!(flags & ProcessInitializationFlags::kNoInitializeCppgc)
+#ifdef __AROS__
+      && !per_process::v8_initialized
+#endif
+  ) {
+    NODE_AROS_PHASE("cppgc-initialize enter");
     v8::PageAllocator* allocator = nullptr;
     if (result->platform_ != nullptr) {
       allocator = result->platform_->GetPageAllocator();
     }
     cppgc::InitializeProcess(allocator);
+    NODE_AROS_PHASE("cppgc-initialize leave");
   }
 
 #if NODE_USE_V8_WASM_TRAP_HANDLER
@@ -1355,6 +1509,27 @@
     ResetSignalHandlers();
   }
 
+#ifdef __AROS__
+  /* Matching half of the re-invocation fix in
+     InitializeOncePerProcessInternal(): do NOT tear down the process-global
+     V8 state here, and do NOT clear per_process::v8_initialized.
+
+     These three disposals are one-way. Upstream only ever reaches them on the
+     way to process exit, so "leaking" them costs nothing there. On AROS
+     node.library outlives the process, NodeStart() returns to its caller, and
+     the next C:Node would then be initializing against a disposed platform -
+     which faulted at NULL+8. This is the same shape as the uv_default_loop
+     problem an earlier tree fixed in c12662e16b ("upstream Node relies on process exit
+     to reclaim uv_default_loop, but NodeStart returns to the caller"), except
+     that for V8 the answer is the opposite: uv state must be released because
+     it holds AROS signals, while V8 state must be KEPT because V8 cannot be
+     re-initialized.
+
+     The per-invocation Isolate is still created and disposed normally, which
+     V8 does support. What survives is the platform, the V8 process state and
+     cppgc's process heap - i.e. exactly what one library load should own. */
+  (void)flags;
+#else
   if (!(flags & ProcessInitializationFlags::kNoInitializeCppgc)) {
     cppgc::ShutdownProcess();
   }
@@ -1380,6 +1555,7 @@
     // will never be fully cleaned up.
     per_process::v8_platform.Dispose();
   }
+#endif  // __AROS__ : process-global V8 state is kept for the library's life
 
 #if HAVE_OPENSSL
   crypto::CleanupCachedRootCertificates();
@@ -1568,14 +1744,19 @@
 }
 
 static ExitCode StartInternal(int argc, char** argv) {
+  NODE_AROS_PHASE("start-internal enter");
   CHECK_GT(argc, 0);
 
   // Hack around with the argv pointer. Used for process.title = "blah".
+  NODE_AROS_PHASE("uv-setup-args enter");
   argv = uv_setup_args(argc, argv);
+  NODE_AROS_PHASE("uv-setup-args leave");
 
+  NODE_AROS_PHASE("initialize-once call");
   std::shared_ptr<InitializationResultImpl> result =
       InitializeOncePerProcessInternal(
           std::vector<std::string>(argv, argv + argc));
+  NODE_AROS_PHASE("initialize-once returned");
   for (const std::string& error : result->errors()) {
     FPrintF(stderr, "%s: %s\n", result->args().at(0), error);
   }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_config.cc node-v22.23.1/src/node_config.cc
--- node-v22.23.1/src/node_config.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_config.cc	2026-07-25 20:42:26.554181304 -0500
@@ -7,6 +7,12 @@
 #include "node_options.h"
 #include "util-inl.h"
 
+#if defined(__AROS__)
+#include <proto/node-v8.h>
+
+#include <vector>
+#endif
+
 #if HAVE_OPENSSL
 #include "ncrypto.h"  // Ensure OPENSSL_IS_BORINGSSL is defined if applicable
 #endif
@@ -24,7 +30,20 @@
 void GetDefaultLocale(const FunctionCallbackInfo<Value>& args) {
   Isolate* isolate = args.GetIsolate();
   Local<Context> context = isolate->GetCurrentContext();
+#if defined(__AROS__)
+  std::string locale;
+  IPTR locale_length = NodeV8GetDefaultLocale(isolate, nullptr, 0);
+  if (locale_length > 0) {
+    std::vector<char> locale_buffer(static_cast<size_t>(locale_length) + 1);
+    if (NodeV8GetDefaultLocale(
+            isolate, locale_buffer.data(), locale_buffer.size()) !=
+        static_cast<IPTR>(-1)) {
+      locale.assign(locale_buffer.data(), static_cast<size_t>(locale_length));
+    }
+  }
+#else
   std::string locale = isolate->GetDefaultLocale();
+#endif
   Local<Value> result;
   if (ToV8Value(context, locale).ToLocal(&result)) {
     args.GetReturnValue().Set(result);
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_constants.cc node-v22.23.1/src/node_constants.cc
--- node-v22.23.1/src/node_constants.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_constants.cc	2026-09-13 00:25:30.074824634 -0500
@@ -43,7 +43,7 @@
 #endif  // !OPENSSL_NO_ENGINE
 #endif  // HAVE_OPENSSL
 
-#if defined(__POSIX__)
+#if defined(__POSIX__) && !defined(__AROS__)
 #include <dlfcn.h>
 #endif
 
@@ -61,6 +61,30 @@
 #include <csignal>
 #include <limits>
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 namespace node {
 
 using v8::Context;
@@ -1280,111 +1304,243 @@
                                 Local<Value> unused,
                                 Local<Context> context,
                                 void* priv) {
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] CreatePerContextProperties begin target=%p context=%p\n",
+          *target,
+          *context);
+#endif
   Isolate* isolate = context->GetIsolate();
   Environment* env = Environment::GetCurrent(context);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] env=%p isolate=%p\n", env, isolate);
+#endif
 
   CHECK(target->SetPrototype(env->context(), Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] target prototype set\n");
+#endif
 
   Local<Object> os_constants = Object::New(isolate);
   CHECK(os_constants->SetPrototype(env->context(),
                                    Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] os object ready\n");
+#endif
 
   Local<Object> err_constants = Object::New(isolate);
   CHECK(err_constants->SetPrototype(env->context(),
                                     Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] errno object ready\n");
+#endif
 
   Local<Object> sig_constants = Object::New(isolate);
   CHECK(sig_constants->SetPrototype(env->context(),
                                     Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] signal object ready\n");
+#endif
 
   Local<Object> priority_constants = Object::New(isolate);
   CHECK(priority_constants->SetPrototype(env->context(),
                                          Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] priority object ready\n");
+#endif
 
   Local<Object> fs_constants = Object::New(isolate);
   CHECK(fs_constants->SetPrototype(env->context(),
                                    Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] fs object ready\n");
+#endif
 
   Local<Object> crypto_constants = Object::New(isolate);
   CHECK(crypto_constants->SetPrototype(env->context(),
                                        Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] crypto object ready\n");
+#endif
 
   Local<Object> zlib_constants = Object::New(isolate);
   CHECK(zlib_constants->SetPrototype(env->context(),
                                      Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] zlib object ready\n");
+#endif
 
   Local<Object> dlopen_constants = Object::New(isolate);
   CHECK(dlopen_constants->SetPrototype(env->context(),
                                        Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] dlopen object ready\n");
+#endif
 
   Local<Object> trace_constants = Object::New(isolate);
   CHECK(trace_constants->SetPrototype(env->context(),
                                       Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] trace object ready\n");
+#endif
 
   Local<Object> internal_constants = Object::New(isolate);
   CHECK(internal_constants->SetPrototype(env->context(),
                                          Null(env->isolate())).FromJust());
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] internal object ready\n");
+#endif
 
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineErrnoConstants enter\n");
+#endif
   DefineErrnoConstants(err_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineErrnoConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineWindowsErrorConstants enter\n");
+#endif
   DefineWindowsErrorConstants(err_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineWindowsErrorConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineSignalConstants enter\n");
+#endif
   DefineSignalConstants(sig_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineSignalConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefinePriorityConstants enter\n");
+#endif
   DefinePriorityConstants(priority_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefinePriorityConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineFsConstants enter\n");
+#endif
   DefineFsConstants(fs_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineFsConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineCryptoConstants enter\n");
+#endif
   DefineCryptoConstants(crypto_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineCryptoConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineZlibConstants enter\n");
+#endif
   DefineZlibConstants(zlib_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineZlibConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineDLOpenConstants enter\n");
+#endif
   DefineDLOpenConstants(dlopen_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineDLOpenConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineTraceConstants enter\n");
+#endif
   DefineTraceConstants(trace_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineTraceConstants leave\n");
+  kprintf("[NodeConstants:AROS] DefineInternalConstants enter\n");
+#endif
   DefineInternalConstants(internal_constants);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] DefineInternalConstants leave\n");
+#endif
 
   // Define libuv constants.
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] define UV_UDP_REUSEADDR enter\n");
+#endif
   NODE_DEFINE_CONSTANT(os_constants, UV_UDP_REUSEADDR);
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] define UV_UDP_REUSEADDR leave\n");
+#endif
 
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export os.dlopen enter\n");
+#endif
   os_constants
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "dlopen"),
             dlopen_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export os.dlopen leave\n");
+  kprintf("[NodeConstants:AROS] export os.errno enter\n");
+#endif
   os_constants
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "errno"),
             err_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export os.errno leave\n");
+  kprintf("[NodeConstants:AROS] export os.signals enter\n");
+#endif
   os_constants
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "signals"),
             sig_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export os.signals leave\n");
+  kprintf("[NodeConstants:AROS] export os.priority enter\n");
+#endif
   os_constants
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "priority"),
             priority_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export os.priority leave\n");
+  kprintf("[NodeConstants:AROS] export target.os enter\n");
+#endif
   target
       ->Set(env->context(), FIXED_ONE_BYTE_STRING(isolate, "os"), os_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.os leave\n");
+  kprintf("[NodeConstants:AROS] export target.fs enter\n");
+#endif
   target
       ->Set(env->context(), FIXED_ONE_BYTE_STRING(isolate, "fs"), fs_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.fs leave\n");
+  kprintf("[NodeConstants:AROS] export target.crypto enter\n");
+#endif
   target
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "crypto"),
             crypto_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.crypto leave\n");
+  kprintf("[NodeConstants:AROS] export target.zlib enter\n");
+#endif
   target
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "zlib"),
             zlib_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.zlib leave\n");
+  kprintf("[NodeConstants:AROS] export target.trace enter\n");
+#endif
   target
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "trace"),
             trace_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.trace leave\n");
+  kprintf("[NodeConstants:AROS] export target.internal enter\n");
+#endif
   target
       ->Set(env->context(),
             FIXED_ONE_BYTE_STRING(isolate, "internal"),
             internal_constants)
       .Check();
+#ifdef __AROS__
+  kprintf("[NodeConstants:AROS] export target.internal leave\n");
+  kprintf("[NodeConstants:AROS] CreatePerContextProperties end\n");
+#endif
 }
 
 }  // namespace constants
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_credentials.cc node-v22.23.1/src/node_credentials.cc
--- node-v22.23.1/src/node_credentials.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_credentials.cc	2026-08-01 22:43:07.224254278 -0500
@@ -12,6 +12,9 @@
 #if !defined(_MSC_VER)
 #include <unistd.h>  // setuid, getuid
 #endif
+#ifdef __AROS__
+#include <proto/usergroup.h>  // setgroups(), initgroups()
+#endif
 #ifdef __linux__
 #include <dlfcn.h>  // dlsym()
 #include <linux/capability.h>
@@ -133,6 +136,19 @@
     return;
   }
 
+#ifdef __AROS__
+  // POSIXC may expose the native T: assignment as /T, /T/, or /T:/... .
+  // Returning that spelling to Node callers eventually sends it back to DOS,
+  // where it is interpreted as a request for a volume literally named "/T".
+  if (dir == "/T" || dir == "/T/") {
+    dir = "T:";
+  } else if (dir.starts_with("/T:/")) {
+    dir.erase(0, 1);
+  } else if (dir.starts_with("/T/")) {
+    dir.replace(0, 3, "T:");
+  }
+#endif
+
   if (dir.size() > 1 && dir.ends_with("/")) {
     dir.pop_back();
   }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_errors.cc node-v22.23.1/src/node_errors.cc
--- node-v22.23.1/src/node_errors.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_errors.cc	2026-09-13 00:25:30.072710242 -0500
@@ -3,6 +3,30 @@
 #include <filesystem>
 #include <sstream>
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 #include "debug_utils-inl.h"
 #include "node_errors.h"
 #include "node_external_reference.h"
@@ -435,6 +459,16 @@
   CHECK(!message.IsEmpty());
   HandleScope scope(isolate);
 
+#ifdef __AROS__
+  {
+    node::Utf8Value detail(
+        isolate,
+        error->ToDetailString(env->context()).FromMaybe(Local<String>()));
+    kprintf("[Node:EXCEPTION] %s\n",
+            detail.length() > 0 ? detail.ToString().c_str() : "(unprintable)");
+  }
+#endif
+
   AppendExceptionLine(env, error, message, FATAL_ERROR);
 
   auto report_to_inspector = [&]() {
@@ -570,6 +604,11 @@
 }
 
 [[noreturn]] void OnFatalError(const char* location, const char* message) {
+#ifdef __AROS__
+  kprintf("[Node:FATAL] location=%s message=%s\n",
+          location != nullptr ? location : "(null)",
+          message != nullptr ? message : "(null)");
+#endif
   if (location) {
     FPrintF(stderr, "FATAL ERROR: %s %s\n", location, message);
   } else {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_internals.h node-v22.23.1/src/node_internals.h
--- node-v22.23.1/src/node_internals.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_internals.h	2026-08-10 05:58:17.445687692 -0500
@@ -143,8 +143,16 @@
   std::atomic<size_t> total_mem_usage_ {0};
 
   // Delegate to V8's allocator for compatibility with the V8 memory cage.
+  // The AROS hosted bringup uses the plain C allocator here because creating
+  // V8's default ArrayBuffer allocator currently trips the hosted TLSF heap
+  // during Electron's owned-main bootstrap.
   std::unique_ptr<v8::ArrayBuffer::Allocator> allocator_{
-      v8::ArrayBuffer::Allocator::NewDefaultAllocator()};
+#if defined(__AROS__)
+      nullptr
+#else
+      v8::ArrayBuffer::Allocator::NewDefaultAllocator()
+#endif
+  };
 };
 
 class DebuggingArrayBufferAllocator final : public NodeArrayBufferAllocator {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_main.cc node-v22.23.1/src/node_main.cc
--- node-v22.23.1/src/node_main.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_main.cc	2026-09-13 00:25:30.076342810 -0500
@@ -22,6 +22,12 @@
 #include "node.h"
 #include <cstdio>
 
+#ifdef __AROS__
+#ifdef NODE_AROS_TRACE
+#include <aros/debug.h>
+#endif
+#endif
+
 #ifdef _WIN32
 #include <windows.h>
 #include <VersionHelpers.h>
@@ -94,6 +100,13 @@
 // UNIX
 
 int main(int argc, char* argv[]) {
+#ifdef __AROS__
+#ifdef NODE_AROS_TRACE
+  bug("[Node] main enter argc=%d\n", argc);
+#endif
+#endif
+  fprintf(stderr, "NODE_MAIN_ENTER argc=%d\n", argc);
+  fflush(stderr);
   return node::Start(argc, argv);
 }
 #endif
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_main_instance.cc node-v22.23.1/src/node_main_instance.cc
--- node-v22.23.1/src/node_main_instance.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_main_instance.cc	2026-09-13 00:25:30.074995603 -0500
@@ -22,6 +22,33 @@
 #include "inspector/worker_inspector.h"  // ParentInspectorHandle
 #endif
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#define NODE_MAIN_TRACE(...) kprintf("[NodeMain] " __VA_ARGS__)
+#else
+#define NODE_MAIN_TRACE(...) ((void) 0)
+#endif
+
 namespace node {
 
 using v8::Context;
@@ -87,6 +114,7 @@
 }
 
 ExitCode NodeMainInstance::Run() {
+  NODE_MAIN_TRACE("Run enter\n");
   Locker locker(isolate_);
   Isolate::Scope isolate_scope(isolate_);
   HandleScope handle_scope(isolate_);
@@ -94,21 +122,46 @@
   ExitCode exit_code = ExitCode::kNoFailure;
   DeleteFnPtr<Environment, FreeEnvironment> env =
       CreateMainEnvironment(&exit_code);
+  NODE_MAIN_TRACE("CreateMainEnvironment env=%p exit=%d\n",
+                  env.get(),
+                  static_cast<int>(exit_code));
   CHECK_NOT_NULL(env);
 
+#ifdef __AROS__
+  SetProcessExitHandler(
+      env.get(), [&exit_code](Environment* exit_env, int requested_exit_code) {
+        exit_code = static_cast<ExitCode>(requested_exit_code);
+        Stop(exit_env, StopFlags::kDoNotTerminateIsolate);
+      });
+#endif
+
   Context::Scope context_scope(env->context());
   Run(&exit_code, env.get());
+  NODE_MAIN_TRACE("Run leave exit=%d\n", static_cast<int>(exit_code));
   return exit_code;
 }
 
 void NodeMainInstance::Run(ExitCode* exit_code, Environment* env) {
   if (*exit_code == ExitCode::kNoFailure) {
     if (!sea::MaybeLoadSingleExecutableApplication(env)) {
-      LoadEnvironment(env, StartExecutionCallback{});
+      NODE_MAIN_TRACE("LoadEnvironment enter\n");
+      auto load_result = LoadEnvironment(env, StartExecutionCallback{});
+      NODE_MAIN_TRACE("LoadEnvironment leave empty=%d\n",
+                      load_result.IsEmpty() ? 1 : 0);
     }
 
-    *exit_code =
-        SpinEventLoopInternal(env).FromMaybe(ExitCode::kGenericUserError);
+    NODE_MAIN_TRACE("SpinEventLoop enter\n");
+    auto loop_result = SpinEventLoopInternal(env);
+    NODE_MAIN_TRACE("SpinEventLoop leave nothing=%d\n",
+                    loop_result.IsNothing() ? 1 : 0);
+    if (loop_result.IsJust()) {
+      *exit_code = loop_result.FromJust();
+    } else if (!env->is_stopping()) {
+      *exit_code = ExitCode::kGenericUserError;
+    } else {
+      NODE_MAIN_TRACE("SpinEventLoop preserved requested exit=%d\n",
+                      static_cast<int>(*exit_code));
+    }
   }
 
 #if defined(LEAK_SANITIZER)
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_messaging.cc node-v22.23.1/src/node_messaging.cc
--- node-v22.23.1/src/node_messaging.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_messaging.cc	2026-09-13 00:25:30.074157327 -0500
@@ -8,8 +8,33 @@
 #include "node_errors.h"
 #include "node_external_reference.h"
 #include "node_process-inl.h"
+#include "node_realm-inl.h"
 #include "util-inl.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 using node::contextify::ContextifyContext;
 using node::errors::TryCatchScope;
 using v8::Array;
@@ -1577,6 +1602,21 @@
 static void SetDeserializerCreateObjectFunction(
     const FunctionCallbackInfo<Value>& args) {
   Environment* env = Environment::GetCurrent(args);
+#ifdef __AROS__
+  Realm* realm = Realm::GetCurrent(args);
+  kprintf("[NodeMessaging:AROS] SetDeserializerCreateObjectFunction env=%p realm=%p argc=%d arg0_is_function=%d\n",
+          env,
+          realm,
+          args.Length(),
+          args.Length() > 0 && args[0]->IsFunction());
+  CHECK(args[0]->IsFunction());
+  if (realm != nullptr) {
+    realm->set_messaging_deserialize_create_object(args[0].As<Function>());
+    kprintf("[NodeMessaging:AROS] SetDeserializerCreateObjectFunction stored via realm\n");
+    return;
+  }
+  kprintf("[NodeMessaging:AROS] SetDeserializerCreateObjectFunction falling back to env principal realm\n");
+#endif
   CHECK(args[0]->IsFunction());
   env->set_messaging_deserialize_create_object(args[0].As<Function>());
 }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_metadata.cc node-v22.23.1/src/node_metadata.cc
--- node-v22.23.1/src/node_metadata.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_metadata.cc	2026-09-12 00:58:06.211790302 -0500
@@ -205,6 +205,13 @@
 #endif  // NODE_HAS_RELEASE_URLS
 }
 
+#ifdef __AROS__
+/* The build configures --dest-os=linux because node's configure.py has no
+   notion of AROS, but the platform string is what scripts actually see.
+   Report the truth: this is AROS, not Linux. */
+Metadata::Metadata() : arch(NODE_ARCH), platform("aros") {}
+#else
 Metadata::Metadata() : arch(NODE_ARCH), platform(NODE_PLATFORM) {}
+#endif
 
 }  // namespace node
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_options-inl.h node-v22.23.1/src/node_options-inl.h
--- node-v22.23.1/src/node_options-inl.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_options-inl.h	2026-09-13 00:25:30.075955467 -0500
@@ -7,6 +7,30 @@
 #include "node_options.h"
 #include "util.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char*, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 namespace node {
 
 PerIsolateOptions* PerProcessOptions::get_per_isolate_options() {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_platform.cc node-v22.23.1/src/node_platform.cc
--- node-v22.23.1/src/node_platform.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_platform.cc	2026-08-10 05:49:47.903665267 -0500
@@ -231,6 +231,15 @@
 WorkerThreadsTaskRunner::WorkerThreadsTaskRunner(
     int thread_pool_size, PlatformDebugLogLevel debug_log_level)
     : debug_log_level_(debug_log_level) {
+#if defined(__AROS__)
+  fprintf(stderr,
+          "[NodePlatform:AROS] foreground-only worker runner requested=%d\n",
+          thread_pool_size);
+  fflush(stderr);
+  (void) thread_pool_size;
+  return;
+#endif
+
   Mutex platform_workers_mutex;
   ConditionVariable platform_workers_ready;
 
@@ -265,19 +274,40 @@
 }
 
 void WorkerThreadsTaskRunner::PostTask(std::unique_ptr<Task> task) {
+#if defined(__AROS__)
+  if (threads_.empty()) {
+    task->Run();
+    return;
+  }
+#endif
   pending_worker_tasks_.Lock().Push(std::move(task));
 }
 
 void WorkerThreadsTaskRunner::PostDelayedTask(std::unique_ptr<Task> task,
                                               double delay_in_seconds) {
+#if defined(__AROS__)
+  if (threads_.empty()) {
+    (void) delay_in_seconds;
+    task->Run();
+    return;
+  }
+#endif
   delayed_task_scheduler_->PostDelayedTask(std::move(task), delay_in_seconds);
 }
 
 void WorkerThreadsTaskRunner::BlockingDrain() {
+#if defined(__AROS__)
+  if (threads_.empty())
+    return;
+#endif
   pending_worker_tasks_.Lock().BlockingDrain();
 }
 
 void WorkerThreadsTaskRunner::Shutdown() {
+#if defined(__AROS__)
+  if (threads_.empty())
+    return;
+#endif
   pending_worker_tasks_.Lock().Stop();
   delayed_task_scheduler_->Stop();
   for (size_t i = 0; i < threads_.size(); i++) {
@@ -286,6 +316,10 @@
 }
 
 int WorkerThreadsTaskRunner::NumberOfWorkerThreads() const {
+#if defined(__AROS__)
+  if (threads_.empty())
+    return 1;
+#endif
   return threads_.size();
 }
 
@@ -437,6 +471,13 @@
   DCHECK_EQ(GetTracingController(), tracing_controller_);
 
   thread_pool_size = GetActualThreadPoolSize(thread_pool_size);
+#if defined(__AROS__)
+  fprintf(stderr,
+          "[NodePlatform:AROS] foreground-only platform requested=%d\n",
+          thread_pool_size);
+  fflush(stderr);
+  return;
+#endif
   worker_thread_task_runner_ = std::make_shared<WorkerThreadsTaskRunner>(
       thread_pool_size, debug_log_level_);
 }
@@ -491,6 +532,13 @@
 void NodePlatform::Shutdown() {
   if (has_shut_down_) return;
   has_shut_down_ = true;
+#if defined(__AROS__)
+  if (!worker_thread_task_runner_) {
+    Mutex::ScopedLock lock(per_isolate_mutex_);
+    per_isolate_.clear();
+    return;
+  }
+#endif
   worker_thread_task_runner_->Shutdown();
 
   {
@@ -500,6 +548,10 @@
 }
 
 int NodePlatform::NumberOfWorkerThreads() {
+#if defined(__AROS__)
+  if (!worker_thread_task_runner_)
+    return 1;
+#endif
   return worker_thread_task_runner_->NumberOfWorkerThreads();
 }
 
@@ -547,6 +599,9 @@
 
   do {
     // Worker tasks aren't associated with an Isolate.
+#if defined(__AROS__)
+    if (worker_thread_task_runner_)
+#endif
     worker_thread_task_runner_->BlockingDrain();
   } while (per_isolate->FlushForegroundTasksInternal());
 }
@@ -614,6 +669,12 @@
     }
     fflush(stderr);
   }
+#if defined(__AROS__)
+  if (!worker_thread_task_runner_) {
+    task->Run();
+    return;
+  }
+#endif
   worker_thread_task_runner_->PostTask(std::move(task));
 }
 
@@ -634,6 +695,13 @@
     }
     fflush(stderr);
   }
+#if defined(__AROS__)
+  if (!worker_thread_task_runner_) {
+    (void) delay_in_seconds;
+    task->Run();
+    return;
+  }
+#endif
   worker_thread_task_runner_->PostDelayedTask(std::move(task),
                                               delay_in_seconds);
 }
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_process_object.cc node-v22.23.1/src/node_process_object.cc
--- node-v22.23.1/src/node_process_object.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_process_object.cc	2026-07-25 20:49:46.332402453 -0500
@@ -75,7 +75,7 @@
 
 static void GetParentProcessId(Local<Name> property,
                                const PropertyCallbackInfo<Value>& info) {
-  info.GetReturnValue().Set(uv_os_getppid());
+  info.GetReturnValue().Set(static_cast<int32_t>(uv_os_getppid()));
 }
 
 static void SetVersions(Isolate* isolate, Local<Object> versions) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_report.cc node-v22.23.1/src/node_report.cc
--- node-v22.23.1/src/node_report.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_report.cc	2026-07-25 20:50:18.920197447 -0500
@@ -15,8 +15,10 @@
 #else  // !_WIN32
 #include <cxxabi.h>
 #include <sys/resource.h>
+#if !defined(__AROS__)
 #include <dlfcn.h>
 #endif
+#endif
 
 #include <cstring>
 #include <ctime>
@@ -273,14 +275,14 @@
   writer->json_keyvalue("nodejsVersion", buf.str());
   buf.str("");
 
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__AROS__)
   // Report compiler and runtime glibc versions where possible.
   const char* (*libc_version)();
   *(reinterpret_cast<void**>(&libc_version)) =
       dlsym(RTLD_DEFAULT, "gnu_get_libc_version");
   if (libc_version != nullptr)
     writer->json_keyvalue("glibcVersionRuntime", (*libc_version)());
-#endif /* _WIN32 */
+#endif /* !_WIN32 && !__AROS__ */
 
 #ifdef __GLIBC__
   buf << __GLIBC__ << "." << __GLIBC_MINOR__;
@@ -734,16 +736,16 @@
     {"core_file_size_blocks", RLIMIT_CORE},
     {"data_seg_size_bytes", RLIMIT_DATA},
     {"file_size_blocks", RLIMIT_FSIZE},
-#if !(defined(_AIX) || defined(__sun))
+#if !(defined(_AIX) || defined(__sun) || defined(__AROS__))
     {"max_locked_memory_bytes", RLIMIT_MEMLOCK},
 #endif
-#ifndef __sun
+#if !defined(__sun) && !defined(__AROS__)
     {"max_memory_size_bytes", RLIMIT_RSS},
 #endif
     {"open_files", RLIMIT_NOFILE},
     {"stack_size_bytes", RLIMIT_STACK},
     {"cpu_time_seconds", RLIMIT_CPU},
-#ifndef __sun
+#if !defined(__sun) && !defined(__AROS__)
     {"max_user_processes", RLIMIT_NPROC},
 #endif
 #ifndef __OpenBSD__
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_sockaddr-inl.h node-v22.23.1/src/node_sockaddr-inl.h
--- node-v22.23.1/src/node_sockaddr-inl.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_sockaddr-inl.h	2026-07-25 20:49:46.328402479 -0500
@@ -31,7 +31,11 @@
 
 bool SocketAddress::is_numeric_host(const char* hostname, int family) {
   in6_addr dst;
+#if defined(__AROS__)
+  return uv_inet_pton(family, hostname, &dst) == 0;
+#else
   return inet_pton(family, hostname, &dst) == 1;
+#endif
 }
 
 int SocketAddress::GetPort(const sockaddr* addr) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_sqlite.cc node-v22.23.1/src/node_sqlite.cc
--- node-v22.23.1/src/node_sqlite.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_sqlite.cc	2026-07-30 19:01:25.796244693 -0500
@@ -1785,6 +1785,13 @@
 }
 
 void DatabaseSync::LoadExtension(const FunctionCallbackInfo<Value>& args) {
+#ifdef __AROS__
+  Environment* env = Environment::GetCurrent(args);
+  return THROW_ERR_DLOPEN_DISABLED(
+      env,
+      "Cannot load SQLite extensions because dynamic loading is unavailable "
+      "on AROS.");
+#else
   DatabaseSync* db;
   ASSIGN_OR_RETURN_UNWRAP(&db, args.This());
   Environment* env = Environment::GetCurrent(args);
@@ -1818,6 +1825,7 @@
   if (r != SQLITE_OK) {
     isolate->ThrowException(ERR_LOAD_SQLITE_EXTENSION(isolate, errmsg));
   }
+#endif
 }
 
 StatementSync::StatementSync(Environment* env,
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_symbols.cc node-v22.23.1/src/node_symbols.cc
--- node-v22.23.1/src/node_symbols.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_symbols.cc	2026-09-13 00:25:30.075403585 -0500
@@ -2,6 +2,30 @@
 #include "node_binding.h"
 #include "util.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 namespace node {
 
 using v8::Context;
@@ -16,6 +40,20 @@
                        Local<Context> context,
                        void* priv) {
   Environment* env = Environment::GetCurrent(context);
+#ifdef __AROS__
+  kprintf("[NodeSymbols:AROS] Initialize begin\n");
+#define V(PropertyName, StringValue)                                           \
+  do {                                                                         \
+    target                                                                     \
+        ->Set(env->context(),                                                  \
+              FIXED_ONE_BYTE_STRING(env->isolate(), StringValue),              \
+              env->PropertyName())                                             \
+        .Check();                                                              \
+  } while (0);
+  PER_ISOLATE_SYMBOL_PROPERTIES(V)
+#undef V
+  kprintf("[NodeSymbols:AROS] Initialize end\n");
+#else
 #define V(PropertyName, StringValue)                                           \
   target                                                                       \
       ->Set(env->context(),                                                    \
@@ -24,6 +62,7 @@
       .Check();
   PER_ISOLATE_SYMBOL_PROPERTIES(V)
 #undef V
+#endif
 }
 
 }  // namespace symbols
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_task_queue.cc node-v22.23.1/src/node_task_queue.cc
--- node-v22.23.1/src/node_task_queue.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_task_queue.cc	2026-09-13 00:25:30.075573753 -0500
@@ -10,6 +10,30 @@
 
 #include <atomic>
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 namespace node {
 
 using errors::TryCatchScope;
@@ -160,27 +184,65 @@
                        Local<Value> unused,
                        Local<Context> context,
                        void* priv) {
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] Initialize begin target=%p\n", *target);
+#endif
   Environment* env = Environment::GetCurrent(context);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] Environment::GetCurrent env=%p\n", env);
+#endif
   Isolate* isolate = env->isolate();
 
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] SetMethod enqueueMicrotask begin\n");
+#endif
   SetMethod(context, target, "enqueueMicrotask", EnqueueMicrotask);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] SetMethod enqueueMicrotask end\n");
+  kprintf("[NodeTaskQueue:AROS] SetMethod setTickCallback begin\n");
+#endif
   SetMethod(context, target, "setTickCallback", SetTickCallback);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] SetMethod setTickCallback end\n");
+  kprintf("[NodeTaskQueue:AROS] SetMethod runMicrotasks begin\n");
+#endif
   SetMethod(context, target, "runMicrotasks", RunMicrotasks);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] SetMethod runMicrotasks end\n");
+  kprintf("[NodeTaskQueue:AROS] export tickInfo begin\n");
+#endif
   target->Set(env->context(),
               FIXED_ONE_BYTE_STRING(isolate, "tickInfo"),
               env->tick_info()->fields().GetJSArray()).Check();
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] export tickInfo end\n");
+  kprintf("[NodeTaskQueue:AROS] create promiseRejectEvents begin\n");
+#endif
 
   Local<Object> events = Object::New(isolate);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] define promise constants begin\n");
+#endif
   NODE_DEFINE_CONSTANT(events, kPromiseRejectWithNoHandler);
   NODE_DEFINE_CONSTANT(events, kPromiseHandlerAddedAfterReject);
   NODE_DEFINE_CONSTANT(events, kPromiseResolveAfterResolved);
   NODE_DEFINE_CONSTANT(events, kPromiseRejectAfterResolved);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] define promise constants end\n");
+#endif
 
   target->Set(env->context(),
               FIXED_ONE_BYTE_STRING(isolate, "promiseRejectEvents"),
               events).Check();
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] export promiseRejectEvents end\n");
+  kprintf("[NodeTaskQueue:AROS] SetMethod setPromiseRejectCallback begin\n");
+#endif
   SetMethod(
       context, target, "setPromiseRejectCallback", SetPromiseRejectCallback);
+#ifdef __AROS__
+  kprintf("[NodeTaskQueue:AROS] Initialize end\n");
+#endif
 }
 
 void RegisterExternalReferences(ExternalReferenceRegistry* registry) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_trace_events.cc node-v22.23.1/src/node_trace_events.cc
--- node-v22.23.1/src/node_trace_events.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_trace_events.cc	2026-09-13 00:25:30.075750113 -0500
@@ -4,10 +4,35 @@
 #include "node.h"
 #include "node_external_reference.h"
 #include "node_internals.h"
+#include "node_realm-inl.h"
 #include "node_v8_platform-inl.h"
 #include "tracing/agent.h"
 #include "util-inl.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 #include <set>
 #include <string>
 
@@ -120,7 +145,16 @@
     const FunctionCallbackInfo<Value>& args) {
   Environment* env = Environment::GetCurrent(args);
   CHECK(args[0]->IsFunction());
+#ifdef __AROS__
+  Realm* realm = Realm::GetCurrent(args);
+  kprintf("[TraceEvents:AROS] SetTraceCategoryStateUpdateHandler env=%p realm=%p\n",
+          env,
+          realm);
+  CHECK_NOT_NULL(realm);
+  realm->set_trace_category_state_function(args[0].As<Function>());
+#else
   env->set_trace_category_state_function(args[0].As<Function>());
+#endif
 }
 
 static void GetCategoryEnabledBuffer(const FunctionCallbackInfo<Value>& args) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_url.cc node-v22.23.1/src/node_url.cc
--- node-v22.23.1/src/node_url.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_url.cc	2026-07-26 08:55:46.470058735 -0500
@@ -122,7 +122,19 @@
 enum class OS { WINDOWS, POSIX };
 
 std::string EncodePathChars(std::string_view input_str, OS operating_system) {
+#ifdef __AROS__
+  /*
+   * AROS absolute paths use a volume prefix (for example "System:lib/node")
+   * rather than a leading slash.  "file://" would make the volume name a URL
+   * host, so retain an empty host with the third slash.
+   */
+  const bool aros_volume_path =
+      operating_system == OS::POSIX && !input_str.empty() &&
+      input_str.front() != '/' && input_str.find(':') != std::string_view::npos;
+  std::string encoded = aros_volume_path ? "file:///" : "file://";
+#else
   std::string encoded = "file://";
+#endif
   encoded.reserve(input_str.size() +
                   7);  // Reserve space for "file://" and input_str
   for (size_t i : input_str) {
@@ -665,7 +677,20 @@
     }
   }
 
-  return ada::unicode::percent_decode(pathname, first_percent);
+  std::string decoded_pathname =
+      ada::unicode::percent_decode(pathname, first_percent);
+#ifdef __AROS__
+  if (decoded_pathname.size() > 2 && decoded_pathname.front() == '/') {
+    size_t volume_end = decoded_pathname.find(':', 1);
+    size_t first_separator = decoded_pathname.find('/', 1);
+    if (volume_end > 1 &&
+        (first_separator == std::string::npos ||
+         volume_end < first_separator)) {
+      decoded_pathname.erase(0, 1);
+    }
+  }
+#endif
+  return decoded_pathname;
 #endif  // _WIN32
 }
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/node_v8.cc node-v22.23.1/src/node_v8.cc
--- node-v22.23.1/src/node_v8.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/node_v8.cc	2026-09-13 00:25:30.073427152 -0500
@@ -29,6 +29,30 @@
 #include "util-inl.h"
 #include "v8.h"
 
+#ifdef __AROS__
+/* AROS: this tracing resolves kprintf() through arossupport's vkprintf(),
+ * which needs _arosdebuglock - a symbol defined INSIDE the kernel ROM
+ * (arch/x86_64-pc/kernel/kernel_debug.c). A disk-loaded library cannot link
+ * against it, so any .library calling kprintf() is unlinkable on a pc target;
+ * hosted only gets away with it because its arossupport uses a vkprintf that
+ * needs no lock. That is why node.library built hosted and had never once
+ * linked for pc-x86_64.
+ *
+ * Tracing is therefore opt-in via NODE_AROS_TRACE. The stub is a static
+ * inline named kprintf, NOT a macro that renames it: renaming rewrote the
+ * next translation unit's own `extern "C" int kprintf(...)` declaration into
+ * a C-linkage declaration of the stub, which clashed with its C++ definition.
+ * The inline still consumes its arguments, so variables that exist only to be
+ * traced do not become unused. Same intent as b066849b for node_bridge.cc.
+ */
+#ifdef NODE_AROS_TRACE
+extern "C" int kprintf(const char* format, ...);
+#elif !defined(NODE_AROS_KPRINTF_STUBBED)
+#define NODE_AROS_KPRINTF_STUBBED 1
+static inline int kprintf(const char*, ...) { return 0; }
+#endif
+#endif
+
 namespace node {
 namespace v8_utils {
 using v8::Array;
@@ -224,6 +248,16 @@
 
 void UpdateHeapCodeStatisticsBuffer(const FunctionCallbackInfo<Value>& args) {
   BindingData* data = Realm::GetBindingData<BindingData>(args);
+#ifdef __AROS__
+  kprintf("[NodeV8:AROS] updateHeapCodeStatisticsBuffer bypass\n");
+  {
+    AliasedFloat64Array& buffer = data->heap_code_statistics_buffer;
+#define V(index, name, _) buffer[index] = 0.0;
+    HEAP_CODE_STATISTICS_PROPERTIES(V)
+#undef V
+  }
+  return;
+#endif
   HeapCodeStatistics s;
   args.GetIsolate()->GetHeapCodeAndMetadataStatistics(&s);
   AliasedFloat64Array& buffer = data->heap_code_statistics_buffer;
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/threadpoolwork-inl.h node-v22.23.1/src/threadpoolwork-inl.h
--- node-v22.23.1/src/threadpoolwork-inl.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/threadpoolwork-inl.h	2026-09-16 12:31:00.000000000 -0500
@@ -30,6 +30,17 @@
 
 namespace node {
 
+#ifdef __AROS__
+/* node.library's bridge: lend this library's per-task base to a thread that
+   never opened it. libuv creates the threadpool workers itself, so node's own
+   thread wrappers never see them - the first thing a work callback touches in
+   node's C runtime then reads a NULL base. GitHub Desktop's renderer hit it
+   through crypto: DeriveBitsJob::DoThreadPoolWork -> ERR_clear_error ->
+   __stdc_geterrnoptr_StdCBase_relwrapper, faulting on a uv worker
+   (2026-09-16). */
+extern "C" void __node_aros_bind_callback_task_base(void);
+#endif
+
 void ThreadPoolWork::ScheduleWork() {
   env_->IncreaseWaitingRequestCounter();
   TRACE_EVENT_NESTABLE_ASYNC_BEGIN0(
@@ -39,6 +50,9 @@
       &work_req_,
       [](uv_work_t* req) {
         ThreadPoolWork* self = ContainerOf(&ThreadPoolWork::work_req_, req);
+#ifdef __AROS__
+        __node_aros_bind_callback_task_base();
+#endif
         TRACE_EVENT_BEGIN0(TRACING_CATEGORY_NODE2(threadpoolwork, sync),
                            self->type_);
         self->DoThreadPoolWork();
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/util.cc node-v22.23.1/src/util.cc
--- node-v22.23.1/src/util.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/util.cc	2026-07-25 20:57:02.845662270 -0500
@@ -323,7 +323,8 @@
 #else  // UNIX, macOS
   struct timeval time_val;
   gettimeofday(&time_val, nullptr);
-  localtime_r(&time_val.tv_sec, tm_struct);
+  time_t seconds = static_cast<time_t>(time_val.tv_sec);
+  localtime_r(&seconds, tm_struct);
 #endif
 }
 
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/tools/js2c.cc node-v22.23.1/tools/js2c.cc
--- node-v22.23.1/tools/js2c.cc	2026-06-23 11:27:45.000000000 -0500
+++ node-v22.23.1/tools/js2c.cc	2026-09-11 22:54:39.346046357 -0500
@@ -182,15 +182,56 @@
 
 %.*s
 namespace {
+#if defined(__AROS__)
+/* AROS: `global_source_map` is a file-scope object of non-trivial type, and
+   its dynamic initialiser is not reliably run before the first builtin lookup
+   on this platform.  Hold the map in a function-local static instead, so it is
+   constructed on first use.  Same idiom as the cppgc statics in V8. */
+const BuiltinSourceMap& ArosGlobalBuiltinSourceMap() {
+  static const BuiltinSourceMap* map = new BuiltinSourceMap {
+#else
 const ThreadsafeCopyOnWrite<BuiltinSourceMap> global_source_map {
+#endif
   BuiltinSourceMap {
 %.*s
   }  // BuiltinSourceMap
+#if defined(__AROS__)
+  };
+  return *map;
+}
+#else
 };  // ThreadsafeCopyOnWrite
+#endif
 }  // anonymous namespace
 
+#if defined(__AROS__)
+/* Definitions for the two entry points node_builtins.cc declares and calls on
+   AROS.  Previously these existed only as hand edits to the GENERATED
+   node_javascript.cc in the out-of-tree earlier build - untracked, and destroyed
+   by any regeneration.  Emitting them from the generator makes the port
+   reproducible. */
+const BuiltinSourceMap& ArosGetBuiltinSourceMapDirect() {
+  return ArosGlobalBuiltinSourceMap();
+}
+
+v8::MaybeLocal<v8::String> ArosLoadBuiltinSourceDirect(v8::Isolate* isolate,
+                                                       const char* id) {
+  const BuiltinSourceMap& source = ArosGlobalBuiltinSourceMap();
+  const auto source_it = source.find(id);
+  if (source_it == source.end()) [[unlikely]] {
+    fprintf(stderr, "Cannot find native builtin: \"%%s\".\n", id);
+    ABORT();
+  }
+  return source_it->second.ToStringChecked(isolate);
+}
+#endif
+
 void BuiltinLoader::LoadJavaScriptSource() {
+#if defined(__AROS__)
+  (void)ArosGlobalBuiltinSourceMap();
+#else
   source_ = global_source_map;
+#endif
 }
 
 void RegisterExternalReferencesForInternalizedBuiltinCode(
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/inspector_agent.cc node-v22.23.1/src/inspector_agent.cc
--- node-v22.23.1/src/inspector_agent.cc	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/inspector_agent.cc	2026-09-15 07:51:18.747663703 -0500
@@ -35,6 +35,9 @@
 
 #include <algorithm>
 #include <cstring>
+#ifdef __AROS__
+#include <mutex>
+#endif
 #include <sstream>
 #include <unordered_map>
 #include <vector>
@@ -62,6 +65,17 @@
 using v8_inspector::V8Inspector;
 using v8_inspector::V8InspectorClient;
 
+#ifdef __AROS__
+// node.library is one shared image for every AROS process that embeds Node,
+// so anything static here is guest-wide rather than per process.  The
+// inspector wake-up handle therefore lives in the owning Agent
+// (Agent::start_io_thread_async_) and SIGUSR1 finds it through this pid
+// registry.  There is no watchdog thread and no semaphore: the handler runs
+// on the signalled process's own task and uv_async_send() is
+// async-signal-safe, so it posts straight to that process's loop.
+static std::mutex start_io_thread_agents_mutex;
+static std::unordered_map<uv_pid_t, Agent*> start_io_thread_agents;
+#else
 #ifdef __POSIX__
 static uv_sem_t start_io_thread_semaphore;
 #endif  // __POSIX__
@@ -71,6 +85,7 @@
 static std::atomic_bool start_io_thread_async_initialized { false };
 // Protects the Agent* stored in start_io_thread_async.data.
 static Mutex start_io_thread_async_mutex;
+#endif  // __AROS__
 
 // Called on the main thread.
 void StartIoThreadAsyncCallback(uv_async_t* handle) {
@@ -78,7 +93,23 @@
 }
 
 
-#ifdef __POSIX__
+#ifdef __AROS__
+static void StartIoThreadWakeup(int signo, siginfo_t* info, void* ucontext) {
+  // A TryLock keeps the handler from blocking on a registry update in
+  // progress; a SIGUSR1 that lands in that window is simply dropped.
+  if (!start_io_thread_agents_mutex.try_lock()) return;
+  auto it = start_io_thread_agents.find(uv_os_getpid());
+  Agent* agent = it != start_io_thread_agents.end() ? it->second : nullptr;
+  start_io_thread_agents_mutex.unlock();
+  if (agent != nullptr && agent->start_io_thread_async_initialized_)
+    uv_async_send(&agent->start_io_thread_async_);
+}
+
+static int StartDebugSignalHandler() {
+  RegisterSignalHandler(SIGUSR1, StartIoThreadWakeup);
+  return 0;
+}
+#elif defined(__POSIX__)
 static void StartIoThreadWakeup(int signo, siginfo_t* info, void* ucontext) {
   uv_sem_post(&start_io_thread_semaphore);
 }
@@ -824,6 +855,47 @@
   host_port_ = host_port;
 
   client_ = std::make_shared<NodeInspectorClient>(parent_env_, is_main);
+#ifdef __AROS__
+  if (parent_env_->owns_inspector()) {
+    // Per-Agent wake-up handle (see the registry note at the top of this
+    // file): a second Node process in the same guest must not trip over the
+    // first one's handle.
+    CHECK(!start_io_thread_async_initialized_);
+    CHECK_EQ(0, uv_async_init(parent_env_->event_loop(),
+                              &start_io_thread_async_,
+                              StartIoThreadAsyncCallback));
+    uv_unref(reinterpret_cast<uv_handle_t*>(&start_io_thread_async_));
+    start_io_thread_async_.data = this;
+    start_io_thread_async_initialized_ = true;
+    {
+      std::lock_guard<std::mutex> lock(start_io_thread_agents_mutex);
+      start_io_thread_agents[uv_os_getpid()] = this;
+    }
+    if (parent_env_->should_start_debug_signal_handler()) {
+      StartDebugSignalHandler();
+    }
+
+    parent_env_->AddCleanupHook([](void* data) {
+      Environment* env = static_cast<Environment*>(data);
+      Agent* agent = env->inspector_agent();
+
+      {
+        std::lock_guard<std::mutex> lock(start_io_thread_agents_mutex);
+        auto it = start_io_thread_agents.find(uv_os_getpid());
+        if (it != start_io_thread_agents.end() && it->second == agent)
+          start_io_thread_agents.erase(it);
+      }
+
+      // The handle is a member of the Agent, which the Environment destroys
+      // only after CleanupHandles() has drained this close.
+      env->CloseHandle(&agent->start_io_thread_async_, [](uv_async_t* handle) {
+        Agent* agent = static_cast<Agent*>(handle->data);
+        CHECK(agent->start_io_thread_async_initialized_);
+        agent->start_io_thread_async_initialized_ = false;
+      });
+    }, parent_env_);
+  }
+#else
   if (parent_env_->owns_inspector()) {
     Mutex::ScopedLock lock(start_io_thread_async_mutex);
     CHECK_EQ(start_io_thread_async_initialized.exchange(true), false);
@@ -852,6 +924,7 @@
       });
     }, parent_env_);
   }
+#endif  // __AROS__
 
   AtExit(parent_env_, [](void* env) {
     Agent* agent = static_cast<Environment*>(env)->inspector_agent();
@@ -1126,6 +1199,16 @@
   if (!options().allow_attaching_debugger) {
     return;
   }
+#ifdef __AROS__
+  CHECK(start_io_thread_async_initialized_);
+  uv_async_send(&start_io_thread_async_);
+  parent_env_->RequestInterrupt([this](Environment*) {
+    StartIoThread();
+  });
+
+  CHECK(start_io_thread_async_initialized_);
+  uv_async_send(&start_io_thread_async_);
+#else
   CHECK(start_io_thread_async_initialized);
   uv_async_send(&start_io_thread_async);
   parent_env_->RequestInterrupt([this](Environment*) {
@@ -1134,6 +1217,7 @@
 
   CHECK(start_io_thread_async_initialized);
   uv_async_send(&start_io_thread_async);
+#endif  // __AROS__
 }
 
 void Agent::ContextCreated(Local<Context> context, const ContextInfo& info) {
diff -ruN -x out -x '.git*' -x node_modules -x config.gypi -x config.status -x config.mk -x icu_config.gypi -x '*.pyc' -x __pycache__ -x '*.pyo' -x '*.pristine' -x '*.orig' -x '*.rej' -x '*.bak' node-v22.23.1/src/inspector_agent.h node-v22.23.1/src/inspector_agent.h
--- node-v22.23.1/src/inspector_agent.h	2026-06-23 11:27:43.000000000 -0500
+++ node-v22.23.1/src/inspector_agent.h	2026-09-15 07:49:57.194160018 -0500
@@ -9,6 +9,9 @@
 
 #include "node_options.h"
 #include "v8.h"
+#ifdef __AROS__
+#include "uv.h"
+#endif
 
 #include <cstddef>
 #include <memory>
@@ -157,6 +160,17 @@
   bool pending_enable_network_tracking = false;
   bool pending_disable_network_tracking = false;
   std::shared_ptr<NetworkResourceManager> network_resource_manager_;
+
+#ifdef __AROS__
+ public:
+  // node.library is one shared image for every AROS process that embeds
+  // Node, so the file-level start_io_thread_async statics in
+  // inspector_agent.cc would be guest-wide: the second Node process to own
+  // an inspector tripped CHECK_EQ(start_io_thread_async_initialized
+  // .exchange(true), false).  The SIGUSR1 wake-up handle lives here instead.
+  uv_async_t start_io_thread_async_;
+  bool start_io_thread_async_initialized_ = false;
+#endif
 };
 
 }  // namespace inspector
